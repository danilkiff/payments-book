\begin{tikzpicture}[x=1cm,y=1cm]
	\tikzset{
		duk/root/.style={fig/mcore, text width=24mm},
		duk/main/.style={fig/mblock, text width=24mm},
		duk/node/.style={
				draw=Ink!80,
				line width=0.45pt,
				rounded corners=2.5pt,
				fill=white,
				minimum width=11mm,
				minimum height=6.5mm,
				inner sep=2pt,
				font=\tiny,
				align=center
			},
		duk/leaf/.style={
				duk/node,
				minimum width=10mm,
				minimum height=5.8mm,
				fill=Panel!45
			}
	}

	\node[duk/root] (bdk) at (0,0) {BDK\\в HSM\\эквайера};
	\node[
		draw=AccentB!55,
		line width=0.75pt,
		densely dashed,
		rounded corners=4pt,
		fit=(bdk),
		inner xsep=5pt,
		inner ysep=5pt
	] {};

	\node[duk/main, text width=22mm, below=5mm of bdk] (ipek) {IPEK\\в терминале};
	\node[
		draw=Ink!55,
		line width=0.75pt,
		densely dashed,
		rounded corners=4pt,
		fit=(ipek),
		inner xsep=5pt,
		inner ysep=5pt
	] (ipekbox) {};

	\node[duk/main, text width=22mm, below=7mm of ipek] (tree) {Дерево\\21 уровней};
	\coordinate (leafrow) at ($(tree.south)+(0,-8mm)$);
	\node[duk/leaf] (k1) at ($(leafrow)+(-5mm,0)$) {Key 1};
	\node[duk/leaf] (k2) at ($(leafrow)+(5mm,0)$) {Key 2};
	\node[duk/leaf] (k3) at ($(leafrow)+(-5mm,-8mm)$) {Key 3};
	\node[duk/leaf] (k4) at ($(leafrow)+(5mm,-8mm)$) {Key N};

	\begin{scope}[on background layer]
		\node[fig/mgroup, fit=(k1)(k2)(k3)(k4), inner xsep=5pt, inner ysep=5pt] (leafbox) {};
	\end{scope}

	\draw[fig/accenta] (bdk.south) -- (ipek.north);
	\draw[fig/arrow] (ipek.south) -- (tree.north);
	\draw[fig/arrow] (tree.south west) -- (k1.north);
	\draw[fig/arrow] (tree.south east) -- (k2.north);
	\draw[fig/arrow] (k1.south) -- (k3.north);
	\draw[fig/arrow] (k2.south) -- (k4.north);

	\draw[AccentB, line width=0.9pt] ($(k1.north west)+(-0.05,0.05)$) -- ($(k1.south east)+(0.05,-0.05)$);
	\draw[AccentB, line width=0.9pt] ($(k1.south west)+(-0.05,-0.05)$) -- ($(k1.north east)+(0.05,0.05)$);
	\draw[AccentB, line width=0.9pt] ($(k2.north west)+(-0.05,0.05)$) -- ($(k2.south east)+(0.05,-0.05)$);
	\draw[AccentB, line width=0.9pt] ($(k2.south west)+(-0.05,-0.05)$) -- ($(k2.north east)+(0.05,0.05)$);

	\node[
		font=\tiny,
		fill=white,
		inner xsep=1pt,
		inner ysep=0.5pt,
		text=AccentA!80!black
	] at ($(bdk.south)!0.5!(ipek.north)+(0.28,0)$) {KSN};
	\node[
		font=\tiny,
		text=AccentB!80!black,
		align=center
	] at ($(ipekbox.south)+(0,-3.5mm)$) {IPEK удаляют\\после init};
	\node[fig/mnote, text width=29mm, below=6mm of leafbox] {DUKPT порождает\\одноразовые\\ключи транзакций.};
\end{tikzpicture}
