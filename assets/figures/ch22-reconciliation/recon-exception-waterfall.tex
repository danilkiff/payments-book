\documentclass[tikz,border=2mm]{standalone}
\input{../templates/figure-preamble}

\begin{document}
\begin{tikzpicture}[x=1cm,y=1cm]
  \tikzset{
    stage/.style={
      fig/mblock,
      minimum width=22mm,
      text width=18mm,
      minimum height=6.8mm,
      inner sep=2.1pt,
      font=\tiny
    },
    decision/.style={
      fig/mdecision,
      text width=17mm,
      aspect=1.6,
      inner sep=1.5pt
    },
    branch/.style={
      fig/mside,
      minimum width=11mm,
      text width=10mm,
      inner sep=1.5pt,
      font=\fontsize{4.8}{5.3}\selectfont
    }
  }

  \node[stage] (incoming) at (0,0) {Клиринговая\\запись};
  \node[decision, below=3.5mm of incoming] (auth) {Есть auth?};
  \node[decision, below=6mm of auth] (amount) {Суммы\\совпали?};
  \node[stage, below=4.5mm of amount] (matched) {Matched\\settlement};

  \node[branch, right=3mm of auth] (unmatched) {Unmatched};
  \node[branch, below=2mm of unmatched] (forcepost) {Force post\\voice?};
  \node[fig/mnote, below=2mm of forcepost, text width=11mm] (forceout)
    {Yes: manual\\No: fraud};

  \node[branch, right=3mm of amount] (mismatch) {Mismatch};
  \node[branch, below=2mm of mismatch] (rules) {Tips /\\delta?};
  \node[fig/mnote, below=2mm of rules, text width=11mm] (rulesout)
    {Yes: auto\\No: manual};

  \node[fig/mnote, below=3mm of matched] (statuses)
    {Statuses: matched, unmatched, mismatch, exception.};

  \draw[fig/accenta] (incoming.south) -- (auth.north);
  \draw[fig/accenta] (auth.south) -- node[right, font=\fontsize{5.2}{5.8}\selectfont, text=AccentA!75!black] {Да} (amount.north);
  \draw[fig/accenta] (amount.south) -- node[right, font=\fontsize{5.2}{5.8}\selectfont, text=Good!70!black] {Да} (matched.north);

  \draw[fig/arrow] (auth.east) -- node[above, font=\fontsize{5.2}{5.8}\selectfont, text=Warn!70!black] {Нет} (unmatched.west);
  \draw[fig/arrow] (unmatched.south) -- (forcepost.north);
  \draw[fig/dashed] (forcepost.south) -- (forceout.north);

  \draw[fig/arrow] (amount.east) -- node[above, font=\fontsize{5.2}{5.8}\selectfont, text=Warn!70!black] {Нет} (mismatch.west);
  \draw[fig/arrow] (mismatch.south) -- (rules.north);
  \draw[fig/dashed] (rules.south) -- (rulesout.north);

  \begin{scope}[on background layer]
    \node[fig/mgroup, fit=(incoming)(auth)(amount)(matched), inner xsep=8pt, inner ysep=8pt] {};
  \end{scope}
\end{tikzpicture}
\end{document}
