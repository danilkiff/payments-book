\begin{tikzpicture}[x=1cm,y=1cm]
  \tikzset{
    flow/step/.style={fig/mblock, text width=24mm},
    flow/core/.style={fig/mcore, text width=24mm}
  }

  \node[flow/step] (holder) at (0,0) {Держатель\\телефона};
  \node[flow/step, below=5mm of holder] (merchant) {Мерчант};
  \node[flow/step, below=5mm of merchant] (acquirer) {Эквайер\\или PSP};
  \node[flow/core, below=5mm of acquirer] (network) {Карточная\\сеть};
  \node[
    fig/mside,
    minimum width=16mm,
    text width=12mm,
    below=4mm of network
  ] (vault) {Token\\Vault};
  \node[flow/step, below=4mm of vault] (issuer) {Эмитент};

  \draw[fig/accenta] (holder.south) -- (merchant.north);
  \draw[fig/accenta] (merchant.south) -- (acquirer.north);
  \draw[fig/accenta] (acquirer.south) -- (network.north);
  \draw[fig/lookup] (network.south) -- (vault.north);
  \draw[fig/accentb] (vault.south) -- (issuer.north);

  \node[
    font=\tiny,
    fill=white,
    inner xsep=1.5pt,
    inner ysep=0.5pt,
    text=AccentA!80!black
  ] at ($(merchant.south)!0.5!(acquirer.north)+(0.18,0)$) {DPAN};
  \node[
    font=\tiny,
    fill=white,
    inner xsep=1pt,
    inner ysep=0.5pt,
    text=AccentB!80!black,
    align=center
  ] at ($(vault.south)!0.5!(issuer.north)+(-0.18,0)$) {FPAN\\+ meta};
  \node[
    font=\tiny,
    text=Muted,
    align=center
  ] at ($(network.south)!0.5!(vault.north)+(8mm,0)$) {DPAN $\rightarrow$\\FPAN};

  \node[fig/mnote, below=6mm of issuer] {Де-токенизация идёт в карточной сети; мерчант и эквайер видят только DPAN.};
\end{tikzpicture}
