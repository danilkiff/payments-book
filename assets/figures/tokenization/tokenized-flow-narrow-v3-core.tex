\documentclass[tikz,border=4mm]{standalone}

\usepackage{fontspec}
\setmainfont{LibertinusSans-Regular.otf}[
  BoldFont=LibertinusSans-Bold.otf,
  ItalicFont=LibertinusSans-Italic.otf,
  BoldItalicFont=LibertinusSans-Bold.otf,
]

\usepackage{xcolor}
\usetikzlibrary{arrows.meta, calc, positioning}

\definecolor{Ink}{HTML}{243042}
\definecolor{Soft}{HTML}{F8FAFD}
\definecolor{Panel}{HTML}{EEF3FA}
\definecolor{TokenBlue}{HTML}{225FCA}
\definecolor{TokenRed}{HTML}{C63F3A}
\definecolor{Muted}{HTML}{5C6775}

\tikzset{
  every node/.style={text=Ink},
  block/.style={
    draw=Ink!90,
    line width=0.5pt,
    rounded corners=2.5pt,
    fill=white,
    minimum width=30mm,
    minimum height=8.8mm,
    inner sep=3pt,
    align=center,
    font=\scriptsize
  },
  networkpanel/.style={
    draw=Ink!90,
    line width=0.6pt,
    rounded corners=4pt,
    fill=Panel,
    minimum width=31mm,
    minimum height=21mm,
    inner sep=0pt
  },
  line/.style={line width=0.45pt, draw=Ink!55},
  dpan/.style={-Latex, line width=1.45pt, draw=TokenBlue},
  fpan/.style={-Latex, line width=1.45pt, draw=TokenRed},
  lookup/.style={<->, line width=0.75pt, draw=Ink!65, densely dashed},
  tag/.style={font=\bfseries\tiny},
  note/.style={
    draw=Ink!18,
    rounded corners=5pt,
    fill=Soft,
    inner sep=5pt,
    text width=31mm,
    align=left,
    font=\tiny,
    text=Muted
  }
}

\begin{document}
\begin{tikzpicture}[x=1cm,y=1cm]
  \node[block] (holder) at (0,0) {Держатель\\(телефон)};
  \node[block, below=7.5mm of holder] (merchant) {Мерчант};
  \node[block, below=7.5mm of merchant] (acquirer) {Эквайер/PSP};
  \node[networkpanel, below=9mm of acquirer] (networkbox) {};
  \node[font=\scriptsize\bfseries] at ($(networkbox.north)+(0,-4.2mm)$) {Карточная сеть};
  \draw[line] ($(networkbox.west)+(1.5mm,0)$) -- ($(networkbox.east)+(-1.5mm,0)$);
  \node[font=\tiny] at ($(networkbox.south)+(0,4.2mm)$) {Token Vault};
  \node[block, below=11mm of networkbox] (issuer) {Эмитент};

  \draw[dpan] (holder.south) -- (merchant.north);
  \draw[dpan] (merchant.south) -- (acquirer.north);
  \draw[dpan] (acquirer.south) -- (networkbox.north);
  \draw[fpan] (networkbox.south) -- (issuer.north);

  \draw[lookup] ($(networkbox.center)+(0,2mm)$) -- ($(networkbox.center)+(0,-2mm)$);

  \node[text=TokenBlue!80!black, tag, anchor=west] at ($(acquirer.east)+(4mm,0)$) {DPAN};
  \node[text=TokenRed!80!black, tag, anchor=west] at ($(networkbox.east)+(4mm,-3mm)$) {FPAN + token metadata};

  \node[note, below=7mm of issuer] {Де-токенизация происходит в карточной сети; мерчант и эквайер видят только DPAN.};
\end{tikzpicture}
\end{document}
