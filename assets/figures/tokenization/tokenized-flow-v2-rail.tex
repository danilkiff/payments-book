\documentclass[tikz,border=4mm]{standalone}

\usepackage{fontspec}
\setmainfont{LibertinusSans-Regular.otf}[
  BoldFont=LibertinusSans-Bold.otf,
  ItalicFont=LibertinusSans-Italic.otf,
  BoldItalicFont=LibertinusSans-Bold.otf,
]

\usepackage{xcolor}
\usetikzlibrary{arrows.meta, calc, positioning, fit, backgrounds}

\definecolor{Ink}{HTML}{243042}
\definecolor{Soft}{HTML}{F7F9FC}
\definecolor{Panel}{HTML}{EEF4FB}
\definecolor{TokenBlue}{HTML}{246BCE}
\definecolor{TokenRed}{HTML}{C8463A}
\definecolor{Muted}{HTML}{5D6B7A}

\tikzset{
  every node/.style={font=\small, text=Ink},
  block/.style={
    draw=Ink!85,
    line width=0.55pt,
    rounded corners=3pt,
    fill=white,
    minimum width=26mm,
    minimum height=11.5mm,
    inner sep=4pt,
    align=center
  },
  network/.style={
    block,
    minimum width=30mm,
    fill=Panel
  },
  vault/.style={
    draw=Ink!65,
    line width=0.5pt,
    rounded corners=3pt,
    fill=Soft,
    minimum width=24mm,
    minimum height=8mm,
    inner sep=3pt,
    align=center
  },
  neutral/.style={-Latex, line width=1.1pt, draw=Ink!55},
  railblue/.style={-Latex, line width=1.6pt, draw=TokenBlue},
  railred/.style={-Latex, line width=1.6pt, draw=TokenRed},
  lookup/.style={<->, line width=0.85pt, draw=Ink!65, densely dashed},
  pillblue/.style={
    fill=TokenBlue!9,
    draw=TokenBlue!45,
    rounded corners=6pt,
    inner xsep=5pt,
    inner ysep=2pt,
    text=TokenBlue!80!black,
    font=\bfseries\scriptsize
  },
  pillred/.style={
    fill=TokenRed!9,
    draw=TokenRed!45,
    rounded corners=6pt,
    inner xsep=5pt,
    inner ysep=2pt,
    text=TokenRed!80!black,
    font=\bfseries\scriptsize
  },
  note/.style={
    text width=14.8cm,
    align=center,
    font=\scriptsize,
    text=Muted
  }
}

\begin{document}
\begin{tikzpicture}[x=1cm,y=1cm]
  \node[block] (holder) at (0,0) {Держатель\\(телефон)};
  \node[block] (merchant) at (3.35,0) {Мерчант};
  \node[block] (acquirer) at (6.95,0) {Эквайер/PSP};
  \node[network] (network) at (10.9,0) {Карточная\\сеть};
  \node[block] (issuer) at (14.8,0) {Эмитент};

  \begin{scope}[on background layer]
    \node[
      draw=Ink!10,
      fill=Soft,
      rounded corners=6pt,
      fit=(holder) (merchant) (acquirer) (network) (issuer),
      inner xsep=9pt,
      inner ysep=10pt
    ] {};
  \end{scope}

  \draw[neutral] (holder.east) -- (merchant.west);
  \draw[neutral] (merchant.east) -- (acquirer.west);
  \draw[neutral] (acquirer.east) -- (network.west);
  \draw[neutral] (network.east) -- (issuer.west);

  \draw[railblue] ($(holder.north east)+(0,0.68)$) -- ($(network.north west)+(0,0.68)$);
  \draw[railred] ($(network.north east)+(0,0.68)$) -- ($(issuer.north west)+(0,0.68)$);

  \node[pillblue] at ($(holder.north east)!0.54!(network.north west)+(0,0.98)$) {DPAN};
  \node[pillred] at ($(network.north east)!0.5!(issuer.north west)+(0,0.98)$) {FPAN + token metadata};

  \node[vault] (vault) at ($(network.south)+(0,-1.55)$) {Token Vault};
  \draw[lookup] (network.south) -- (vault.north);
  \node[font=\scriptsize, text=Muted, align=center] at ($(vault.east)+(1.15,0)$) {поиск\\DPAN $\rightarrow$ FPAN};

  \node[note] at (7.4,-2.95) {Де-токенизация происходит в карточной сети; мерчант и эквайер видят только DPAN.};
\end{tikzpicture}
\end{document}
