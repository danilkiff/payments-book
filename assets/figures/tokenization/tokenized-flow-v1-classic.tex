\documentclass[tikz,border=4mm]{standalone}

\usepackage{fontspec}
\setmainfont{LibertinusSans-Regular.otf}[
  BoldFont=LibertinusSans-Bold.otf,
  ItalicFont=LibertinusSans-Italic.otf,
  BoldItalicFont=LibertinusSans-Bold.otf,
]

\usepackage{xcolor}
\usetikzlibrary{arrows.meta, calc, positioning}

\definecolor{Ink}{HTML}{243042}
\definecolor{Soft}{HTML}{F6F8FB}
\definecolor{Panel}{HTML}{EEF3F9}
\definecolor{TokenBlue}{HTML}{1F6FEB}
\definecolor{TokenRed}{HTML}{C93C37}
\definecolor{Muted}{HTML}{5D6B7A}

\tikzset{
  every node/.style={font=\small, text=Ink},
  block/.style={
    draw=Ink,
    line width=0.6pt,
    rounded corners=3pt,
    fill=Soft,
    minimum width=27mm,
    minimum height=12mm,
    inner sep=4pt,
    align=center
  },
  network/.style={
    block,
    minimum width=31mm,
    fill=Panel
  },
  vault/.style={
    draw=Ink!70,
    line width=0.5pt,
    rounded corners=3pt,
    fill=white,
    minimum width=25mm,
    minimum height=8.5mm,
    inner sep=3pt,
    align=center
  },
  flow/.style={-Latex, line width=1.35pt, draw=Ink},
  dpan/.style={-Latex, line width=1.45pt, draw=TokenBlue},
  fpan/.style={-Latex, line width=1.45pt, draw=TokenRed},
  lookup/.style={<->, line width=0.9pt, draw=Ink!70, dashed},
  tagblue/.style={
    fill=TokenBlue!10,
    draw=TokenBlue!55,
    rounded corners=6pt,
    inner xsep=5pt,
    inner ysep=2.5pt,
    text=TokenBlue!75!black,
    font=\bfseries\scriptsize
  },
  tagred/.style={
    fill=TokenRed!10,
    draw=TokenRed!55,
    rounded corners=6pt,
    inner xsep=5pt,
    inner ysep=2.5pt,
    text=TokenRed!75!black,
    font=\bfseries\scriptsize
  },
  note/.style={
    draw=Ink!20,
    fill=Soft,
    rounded corners=4pt,
    inner sep=6pt,
    align=center,
    text width=14.8cm,
    font=\scriptsize,
    text=Muted
  }
}

\begin{document}
\begin{tikzpicture}[x=1cm,y=1cm]
  \node[block] (holder) at (0,0) {Держатель\\(телефон)};
  \node[block] (merchant) at (3.35,0) {Мерчант};
  \node[block] (acquirer) at (6.95,0) {Эквайер/PSP};
  \node[network] (network) at (10.9,0) {Карточная\\сеть};
  \node[block] (issuer) at (14.8,0) {Эмитент};

  \draw[dpan] (holder.east) -- (merchant.west);
  \draw[dpan] (merchant.east) -- (acquirer.west);
  \draw[dpan] (acquirer.east) -- (network.west);
  \draw[fpan] (network.east) -- (issuer.west);

  \node[tagblue] at ($(holder.east)!0.5!(network.west)+(0,0.92)$) {DPAN};
  \node[tagred] at ($(network.east)!0.5!(issuer.west)+(0,0.92)$) {FPAN + token metadata};

  \node[vault] (vault) at ($(network.south)+(0,-1.55)$) {Token Vault};
  \draw[lookup] (network.south) -- node[right=5pt, align=left, font=\scriptsize, text=Muted] {поиск\\DPAN $\rightarrow$ FPAN} (vault.north);

  \node[note] at (7.4,-3.0) {Де-токенизация происходит в карточной сети; мерчант и эквайер видят только DPAN.};
\end{tikzpicture}
\end{document}
