\documentclass[tikz,border=4mm]{standalone}

\usepackage{fontspec}
\setmainfont{LibertinusSans-Regular.otf}[
  BoldFont=LibertinusSans-Bold.otf,
  ItalicFont=LibertinusSans-Italic.otf,
  BoldItalicFont=LibertinusSans-Bold.otf,
]

\usepackage{xcolor}
\usetikzlibrary{arrows.meta, calc, positioning}

\definecolor{Ink}{HTML}{243042}
\definecolor{Soft}{HTML}{F7F9FC}
\definecolor{Panel}{HTML}{EEF4FB}
\definecolor{TokenBlue}{HTML}{1F6FEB}
\definecolor{TokenRed}{HTML}{C93C37}
\definecolor{Muted}{HTML}{5D6B7A}

\tikzset{
  every node/.style={text=Ink},
  block/.style={
    draw=Ink,
    line width=0.55pt,
    rounded corners=3pt,
    fill=Soft,
    minimum width=32mm,
    minimum height=9.5mm,
    inner sep=3.5pt,
    align=center,
    font=\scriptsize
  },
  network/.style={
    block,
    fill=Panel,
    minimum height=10.5mm
  },
  vault/.style={
    draw=Ink!70,
    line width=0.45pt,
    rounded corners=3pt,
    fill=white,
    minimum width=20mm,
    minimum height=7.5mm,
    inner sep=2.5pt,
    align=center,
    font=\scriptsize
  },
  dpan/.style={-Latex, line width=1.35pt, draw=TokenBlue},
  fpan/.style={-Latex, line width=1.35pt, draw=TokenRed},
  lookup/.style={<->, line width=0.8pt, draw=Ink!65, dashed},
  tagblue/.style={
    fill=TokenBlue!10,
    draw=TokenBlue!45,
    rounded corners=6pt,
    inner xsep=4pt,
    inner ysep=2pt,
    text=TokenBlue!80!black,
    font=\bfseries\tiny
  },
  tagred/.style={
    fill=TokenRed!10,
    draw=TokenRed!45,
    rounded corners=6pt,
    inner xsep=4pt,
    inner ysep=2pt,
    text=TokenRed!80!black,
    font=\bfseries\tiny
  },
  note/.style={
    draw=Ink!18,
    fill=Soft,
    rounded corners=4pt,
    inner sep=5pt,
    align=left,
    text width=31mm,
    font=\tiny,
    text=Muted
  }
}

\begin{document}
\begin{tikzpicture}[x=1cm,y=1cm]
  \node[block] (holder) at (0,0) {Держатель\\(телефон)};
  \node[block, below=8mm of holder] (merchant) {Мерчант};
  \node[block, below=8mm of merchant] (acquirer) {Эквайер/PSP};
  \node[network, below=8mm of acquirer] (network) {Карточная сеть};
  \node[block, below=11mm of network] (issuer) {Эмитент};

  \draw[dpan] (holder.south) -- (merchant.north);
  \draw[dpan] (merchant.south) -- (acquirer.north);
  \draw[dpan] (acquirer.south) -- (network.north);
  \draw[fpan] (network.south) -- (issuer.north);

  \node[tagblue, anchor=west] at ($(merchant.east)+(3mm,7mm)$) {DPAN};
  \node[tagred, anchor=west] at ($(issuer.east)+(3mm,7mm)$) {FPAN + token metadata};

  \node[vault, right=7mm of network] (vault) {Token\\Vault};
  \draw[lookup] (network.east) -- node[above, font=\tiny, text=Muted] {DPAN $\rightarrow$ FPAN} (vault.west);

  \node[note, below=8mm of issuer] (note) {Де-токенизация происходит в карточной сети; мерчант и эквайер видят только DPAN.};
\end{tikzpicture}
\end{document}
