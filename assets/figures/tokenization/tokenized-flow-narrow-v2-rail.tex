\documentclass[tikz,border=4mm]{standalone}

\usepackage{fontspec}
\setmainfont{LibertinusSans-Regular.otf}[
  BoldFont=LibertinusSans-Bold.otf,
  ItalicFont=LibertinusSans-Italic.otf,
  BoldItalicFont=LibertinusSans-Bold.otf,
]

\usepackage{xcolor}
\usetikzlibrary{arrows.meta, calc, positioning, fit, backgrounds}

\definecolor{Ink}{HTML}{243042}
\definecolor{Soft}{HTML}{F7F9FC}
\definecolor{Panel}{HTML}{EEF4FB}
\definecolor{TokenBlue}{HTML}{246BCE}
\definecolor{TokenRed}{HTML}{C8463A}
\definecolor{Muted}{HTML}{5D6B7A}

\tikzset{
  every node/.style={text=Ink},
  block/.style={
    draw=Ink!85,
    line width=0.5pt,
    rounded corners=3pt,
    fill=white,
    minimum width=31mm,
    minimum height=9mm,
    inner sep=3pt,
    align=center,
    font=\scriptsize
  },
  network/.style={
    block,
    minimum height=10mm,
    fill=Panel
  },
  vault/.style={
    draw=Ink!65,
    line width=0.45pt,
    rounded corners=3pt,
    fill=Soft,
    minimum width=19mm,
    minimum height=7mm,
    inner sep=2.5pt,
    align=center,
    font=\tiny
  },
  railblue/.style={-Latex, line width=1.5pt, draw=TokenBlue},
  railred/.style={-Latex, line width=1.5pt, draw=TokenRed},
  lookup/.style={<->, line width=0.8pt, draw=Ink!65, densely dashed},
  pillblue/.style={
    fill=TokenBlue!9,
    draw=TokenBlue!45,
    rounded corners=6pt,
    inner xsep=4pt,
    inner ysep=2pt,
    text=TokenBlue!80!black,
    font=\bfseries\tiny
  },
  pillred/.style={
    fill=TokenRed!9,
    draw=TokenRed!45,
    rounded corners=6pt,
    inner xsep=4pt,
    inner ysep=2pt,
    text=TokenRed!80!black,
    font=\bfseries\tiny
  },
  note/.style={
    text width=31mm,
    align=left,
    font=\tiny,
    text=Muted
  }
}

\begin{document}
\begin{tikzpicture}[x=1cm,y=1cm]
  \node[block] (holder) at (0,0) {Держатель\\(телефон)};
  \node[block, below=7.5mm of holder] (merchant) {Мерчант};
  \node[block, below=7.5mm of merchant] (acquirer) {Эквайер/PSP};
  \node[network, below=7.5mm of acquirer] (network) {Карточная сеть};
  \node[block, below=10.5mm of network] (issuer) {Эмитент};

  \begin{scope}[on background layer]
    \node[
      draw=Ink!10,
      fill=Soft,
      rounded corners=6pt,
      fit=(holder) (merchant) (acquirer) (network) (issuer),
      inner xsep=9pt,
      inner ysep=10pt
    ] {};
  \end{scope}

  \draw[railblue] ($(holder.south)+(0,0)$) -- ($(network.north)+(0,0)$);
  \draw[railred] ($(network.south)+(0,0)$) -- ($(issuer.north)+(0,0)$);

  \node[pillblue, anchor=west] at ($(holder.east)+(4mm,-1mm)$) {DPAN};
  \node[pillred, anchor=west] at ($(network.east)+(4mm,-1mm)$) {FPAN + token metadata};

  \node[vault, right=7mm of network] (vault) {Token\\Vault};
  \draw[lookup] (network.east) -- (vault.west);
  \node[font=\tiny, text=Muted, anchor=north west] at ($(vault.south west)+(0,-1.5mm)$) {поиск DPAN $\rightarrow$ FPAN};

  \node[note, below=7mm of issuer] {Де-токенизация происходит в карточной сети; мерчант и эквайер видят только DPAN.};
\end{tikzpicture}
\end{document}
