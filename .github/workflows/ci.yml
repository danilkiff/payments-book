name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    name: Build PDF
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check texindy
        uses: docker://ghcr.io/xu-cheng/texlive-alpine:20260101
        with:
          entrypoint: texindy
          args: -V

      - name: Compile LaTeX
        uses: docker://ghcr.io/xu-cheng/texlive-alpine:20260101
        with:
          entrypoint: latexmk
          args: -xelatex payments-book.tex
