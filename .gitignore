# ── Артефакты сборки LaTeX ──────────────────────────────────────────────────
build/
src/gitversion.tex

# ── Генерируемые фигуры (Inkscape *.svg коммитятся; *.pdf пересобирается через make svg) ─
*.pdf
*.aux
*.bbl
*.bcf
*.blg
*.log
*.run.xml
*.synctex.gz
*.toc

# ── macOS ────────────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ── VSCode (оставляем .vscode/ в репо, но игнорируем личные настройки) ──────
.vscode/*.log

# ── Временные файлы редакторов ───────────────────────────────────────────────
*~
*.swp
*.bak

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__

# ── Черновики (design-notes для фигур, локально) ─────────────────────────────
docs/

