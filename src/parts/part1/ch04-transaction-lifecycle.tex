
\chapter{Жизнь транзакции: от касания карты до денег на счёте}\label{ch:tx-lifecycle}
Покупатель прикладывает карту в кофейне, терминал показывает
\enquote{одобрено}, но деньги продавец увидит на счёте только
позже. Карточная транзакция -- многофазная операция, и на каждом
шаге меняется то, что уже стало фактом в системе, а что пока
остаётся обещанием или требованием.

\section{Авторизация}\label{sec:tx-authorization}

Авторизация -- единственная фаза карточного платежа, которая
происходит в реальном времени и видна покупателю: терминал
показывает \enquote{одобрено} или \enquote{отказано}, и на этом
взаимодействие покупателя с платёжной системой заканчивается.
Всё остальное, клиринг, расчёт, возможный диспут, происходит
за кулисами, иногда через дни после покупки. Но именно
в эти несколько секунд авторизации фиксируются все данные, по которым строится вся дальнейшая обработка транзакции.

\subsection*{Путь авторизационного запроса}

Когда держатель прикладывает карту к терминалу (или вставляет
её, или проводит магнитной полосой), терминал считывает данные
карты и формирует авторизационный запрос: структурированное
сообщение в формате ISO~8583 (подробно о формате см.\
в главе~\ref{ch:iso8583}). Внутри такого сообщения собраны как
минимум PAN (DE~2) и данные Track 2 (DE~35) или их чиповый
эквивалент (DE~55), сумма и валюта операции (DE~4, DE~49),
идентификаторы терминала и ТСП (DE~41, DE~42), категория
ТСП MCC (DE~18) и тип операции, который задают MTI
и processing code (DE~3).

Сформированное сообщение отправляется эквайеру, банку или процессору,
обслуживающему этот терминал. Эквайер выполняет первичные проверки:
валидность терминала, соответствие MCC, базовые антифрод-правила
на стороне эквайера, и маршрутизирует запрос в карточную
сеть~\cite{visa-core-rules}. Карточная сеть, в свою очередь,
по IIN/BIN определяет эмитента (или его процессор) и передаёт
запрос ему.

Весь этот путь, терминал, эквайер, карточная сеть, эмитент,
и обратно должен уложиться в жёсткие временны\'{е} рамки. У Visa публичные правила прямо задают верхние пределы
ожидания. Для POS-авторизаций это 10~секунд в большинстве
регионов и 5~секунд в Европе; для снятия наличных в банкомате
25~секунд в большинстве регионов и 5~секунд в Европе. Если
ответ не пришёл вовремя, Visa может ответить за эмитента
через механизм Stand-In Processing (STIP); кроме того, ТСП или эквайер
не должен таймаутить POS-транзакцию раньше чем через 15~секунд.
STIP одобряет только операции с низким профилем риска: сумма
в пределах нормы для данной карты и ТСП, знакомая география,
отсутствие сигналов фрода. Это консервативнее, чем решение
эмитента: сеть предпочитает отклонить неясную операцию, а не
принять риск убытка на себя. STIP не заменяет
эмитента: он покрывает только безопасный хвост
распределения транзакций.
У Mastercard аналогичные пределы ожидания и показатели
доступности задаются в спецификациях сообщений и контролируются
через нормативы доступности авторизации~\cite{visa-core-rules,mc-tpr}.

\subsection*{Решение эмитента}

Эмитент (или его процессор), получив авторизационный запрос,
решает, одобрить или отклонить, на основе
нескольких групп проверок, которые выполняются за доли секунды:

\begin{enumerate}
  \item \textbf{Проверка карты.} Существует ли карта с таким PAN?
    Не заблокирована ли она? Не истёк ли срок действия?
    Совпадает ли CVV или криптограмма?
  \item \textbf{Проверка баланса.} Достаточно ли средств на счёте
    (для дебетовой карты) или доступного кредитного лимита
    (для кредитной)?
  \item \textbf{Лимиты и ограничения.} Не превышен ли дневной лимит
    на операции? Не заблокирована ли карта для этого типа
    операций (например, запрет на интернет-покупки)? Не ограничена ли
    география использования?
  \item \textbf{Антифрод.} Соответствует ли операция типичному
    поведению держателя? Нет ли признаков компрометации карты?
    Не входит ли ТСП в списки повышенного риска?
  \item \textbf{Аутентификация держателя.} Для чиповых транзакций:
    верификация криптограммы (ARQC); для CNP: результат
    3-D Secure, если он был; для магнитной полосы: CVV.
\end{enumerate}

По результатам этих проверок эмитент формирует ответ с кодом
ответа (response code, DE~39): \texttt{00}: операция одобрена,
\texttt{05}: общий отказ без детализации, \texttt{51}: недостаточно
средств, \texttt{14}: неверный номер карты, и так далее; полный список
кодов ответа зависит от конкретного ISO~8583-профиля и схемных
документов: базовая модель стандартизована, но сети дополняют её
собственными значениями и уточнениями по обработке~\cite{iso-8583-2023,visa-tadg,mc-rules}.

Если транзакция одобрена, эмитент присваивает ей код авторизации
(authorization code, DE~38), шестисимвольный идентификатор,
который привязывает одобрение к конкретному запросу
и будет использоваться на этапе клиринга для сопоставления
авторизации с финансовой записью. Одновременно эмитент блокирует
(hold) запрошенную сумму на счёте держателя: деньги ещё
не списаны, но уже недоступны для других операций.

\subsection*{Срок жизни hold}

Hold, блокировка суммы на счёте держателя, не бессрочен,
но и не живёт по единому универсальному таймеру. Точнее говорить
об окне между авторизацией и тем
моментом, когда транзакцию нужно либо представить в clearing,
либо снять через reversal. У Visa для большинства
транзакций с физическим предъявлением карты это окно обычно
составляет 5 календарных дней, для операций без предъявления
карты 10 дней, а для оценочной авторизации в гостиницах,
круизах и аренде автомобилей до 30 дней. У Mastercard
clearing по обычной авторизации обычно должен прийти в пределах
7 календарных дней, а по предавторизации в пределах 30 дней.
На практике именно эти
окна и задают, как долго эмитент будет держать неиспользованный
резерв, хотя точный момент снятия hold зависит ещё и от внутренних
правил эмитента~\cite{visa-core-rules,mc-tpr}.

Числа 5/7/30 дней отражают минимальный операционный цикл: время
доставки или оказания услуги плюс время передачи клирингового
файла от эквайера в сеть плюс буфер на задержки между участниками.
Более длинные окна для гостиниц и аренды автомобилей (30 дней)
соответствуют реальной дельте между check-in-авторизацией и финальной
суммой при выезде. Сроки исторически сложились и поддерживаются
консенсусом схем: изменение сроков требует согласования
всех участников расчётной цепочки.

\practicenote{%
Если клиент жалуется, что \enquote{деньги списали, но покупка
не прошла}, чаще всего речь идёт о hold. Авторизация была
одобрена и средства заблокированы, но по какой-то
причине продавец не завершил операцию (например, отменил заказ
на своей стороне, не отправив reversal). Деньги вернутся
автоматически по истечении hold, но клиенту это не очевидно,
и он видит в мобильном банке уменьшенный доступный баланс.
}

\practicenote{%
Частичный отказ авторизационного хоста эмитента создаёт
рассинхронизацию hold и реального состояния счёта. Если запрос
был одобрен до отказа, но обновление hold-таблицы не сохранилось,
сумма остаётся заблокированной на неопределённый срок, хотя
клиент видит \enquote{отказ} в мобильном банке. Противоположный сценарий:
hold снят слишком рано при откате незафиксированной транзакции --
клиент тратит средства, которые ещё pending. Для этого класса
расхождений операционные команды ведут отдельный
журнал hold-записей с timestamp авторизации и признаком clearing-match.

}

\section{Клиринг}\label{sec:tx-clearing}

Если авторизация означает обещание заплатить, то клиринг означает
предъявление счёта: формальное требование эквайера к эмитенту
исполнить данное обещание. Клиринг происходит не в реальном
времени, а пакетами (batches), и его механика принципиально
отличается от авторизации: здесь нет мгновенного ответа,
нет покупателя, ожидающего результат, и нет жёстких таймаутов
в несколько секунд.

\subsection*{Формирование клирингового файла}

В конце операционного дня (или несколько раз в день, в зависимости
от настроек) эквайер формирует клиринговый файл (clearing file):
пакет записей обо всех транзакциях, прошедших авторизацию за период.
Каждая запись содержит реквизиты транзакции: PAN, сумму (которая
может отличаться от авторизованной, об этом ниже), валюту, дату
и время, идентификатор ТСП, MCC, код авторизации и другие
данные, необходимые для расчёта~\cite{mc-rules}.
Отправку такого файла с итоговыми суммами называют финансовым
предъявлением (financial presentment) или захватом (capture):
в этот момент эквайер фиксирует финальную сумму обязательства~\cite{visa-tadg,mc-tpr}.

Формат клирингового файла зависит от карточной сети. Visa
использует формат BASE II, в котором каждый тип записи
обозначается Transaction Code (TC); Mastercard использует формат IPM
(Integrated Product Messages); НСПК использует собственный формат для внутрироссийских
операций. Несмотря на различия в форматах, логика одна и та же:
набор записей с реквизитами транзакций, которые сеть должна
обработать и по которым нужно произвести расчёт~\cite{visa-core-rules,mc-rules}.

\subsection*{Сопоставление с авторизацией}

Получив клиринговый файл, карточная сеть сопоставляет каждую
клиринговую запись с соответствующей авторизацией по комбинации
PAN, суммы, кода авторизации и даты. Такое сопоставление
не всегда тривиально, потому что сумма в клиринге может
отличаться от авторизованной. Типичный пример: ресторан:
авторизация проходит на сумму заказа, а в клиринг попадает сумма
с чаевыми, которые клиент добавил позже. Правила схем допускают
такое расхождение только для определённых сценариев, например,
чаевых или категорий, связанных с поездками и развлечениями,
а точные пределы зависят от MCC, программы и текущей редакции правил~\cite{visa-core-rules,mc-rules}.

Если предъявление в clearing приходит позже применимого окна
или данные в нём
уже не бьются с исходной авторизацией, сеть всё равно может
доставить это предъявление эмитенту. Но для эквайера это уже зона
late presentment: повышается риск диспута именно по этой причине,
а эквайер теряет часть защитного временного окна по спору~\cite{visa-core-rules,mc-tpr,mc-chargeback-guide}.

Ещё острее ситуация, когда клиринговый файл теряется
или не принимается сетью из-за ошибки формата.
Обнаруживает проблему эквайер: сеть
возвращает отчёт об обработке с перечнем отклонённых записей
(rejected transactions). Эквайер исправляет записи и повторно
отправляет файл в следующем батч-окне. Формально правила
сетей ограничивают срок повторной подачи: у Visa financial
presentment должен попасть в сеть в пределах авторизационного
окна (5--30 дней в зависимости от MCC), иначе авторизация
аннулируется~\cite{visa-core-rules}. У Mastercard clearing
должен прийти в пределах 7--30 календарных дней~\cite{mc-tpr}.
Если эквайер не уложился, hold снимается автоматически
со стороны эмитента: деньги возвращаются в доступный баланс
держателя. Для продавца это означает, что операция
прошла в терминале, товар отдан, но деньги не будут получены
через карточную сеть -- взыскивать придётся иначе.
Поэтому эквайер мониторит статус каждого клирингового батча.

Если же авторизации не существовало вовсе, эмитент всё равно
проводит клиринговую запись по счёту держателя -- без предварительного
hold'а: деньги списываются в момент поступления записи, а не при
авторизации. Риск переходит к эквайеру и продавцу; именно этот
сценарий описывает force post (раздел~\ref{sec:tx-edge-cases})~\cite{mc-tpr}.

\subsection*{Расчёт interchange и комиссий}

На этапе клиринга карточная сеть рассчитывает interchange
для каждой транзакции: ту самую межбанковскую комиссию, которую
эквайер платит эмитенту (глава~\ref{ch:ecosystem}). Ставка interchange зависит
от множества параметров, зафиксированных в клиринговой записи:
типа карты (дебетовая, кредитная, корпоративная), MCC,
способа приёма (чип, магнитная полоса, CNP), географии
(внутренняя или трансграничная операция) и ряда
дополнительных признаков.

Одновременно сеть начисляет и собственные комиссии: плату
за обработку, за трансграничную маршрутизацию, за использование
специальных сервисов. В схемных документах этот слой часто
обозначают как scheme fees и assessments. По итогам обработки
всех клиринговых записей за период сеть формирует чистые расчётные
позиции (net settlement positions) для каждого банка-участника,
итоговые суммы, которые один банк должен другому с учётом
взаимозачёта встречных потоков.

\section{Межбанковский расчёт (settlement)}\label{sec:tx-settlement}

Settlement завершает жизненный цикл транзакции: именно здесь деньги
действительно двигаются между банками. До этого момента средства
не покидали счёт держателя карты: авторизация лишь заблокировала
сумму, клиринг сформировал требование. На этапе settlement это
требование исполняется в расчётной инфраструктуре платёжной
системы~\cite{fz-161,visa-core-rules,mc-tpr}.

\subsection*{Нетто-расчёт}

Карточные сети используют нетто-расчёт (net settlement):
вместо того чтобы проводить отдельную межбанковскую операцию
для каждой транзакции, сеть суммирует все встречные обязательства
между парами банков за расчётный период и переводит только
разницу. Если за день банк А как эквайер должен банку Б как
эмитенту миллион рублей interchange, а банк Б как эквайер должен
банку А как эмитенту восемьсот тысяч, фактически переводятся
только двести тысяч от А к Б. Этот механизм многократно сокращает
число и объём межбанковских проводок.

Карточная сеть проводит расчёт через расчётный банк (settlement bank),
назначенный сетью, или через иной расчётный сервис,
предусмотренный её правилами. У Visa локальные операции,
если в стране доступен соответствующий контур, проходят через
National Net Settlement Service (NNSS), а остальные через
International Settlement Service; правила также отдельно определяют
расчётный банк и расчётный счёт участника. У Mastercard
итоговым документом дня становится ежедневное уведомление
по нетто-расчётам (daily net settlement advisement): именно оно
считается окончательной записью о движении средств между участниками~\cite{visa-core-rules,mc-tpr}.

\subsection*{Когда продавец получает деньги}

С точки зрения продавца settlement начинается в тот момент, когда деньги
за проданный товар или оказанную услугу поступают на его расчётный
счёт. Однако между расчётом в карточной сети и зачислением на счёт
продавца может быть ещё один шаг: эквайер, получив средства от сети,
перечисляет их продавцу в соответствии с условиями договора
эквайринга: ежедневно, через день или по иному графику,
за вычетом MSC.

Универсального для рынка срока здесь нет: после расчёта в сети
эквайер перечисляет деньги продавцу по графику, зафиксированному
в договоре эквайринга, и на практике это может быть следующий
рабочий день или иной срок по SLA конкретного провайдера.
Для продавца, работающего с большим оборотом или узкой маржой,
каждый дополнительный день означает замороженные оборотные средства,
поэтому скорость settlement
часто становится предметом переговоров с эквайером.

\begin{figure}[tbp]
  \centering
  \includefiguresvg[width=\linewidth]{assets/figures/ch04-transaction-lifecycle/tx-lifecycle-timeline}
  \caption{Жизненный цикл карточной транзакции: от касания карты до
  зачисления продавцу.}\label{fig:tx-lifecycle-timeline}
\end{figure}

\section{Двухсообщная и односообщная схема}\label{sec:tx-dual-single}

Описанная выше схема -- двухсообщная (dual-message): авторизация
и клиринг идут разными сообщениями в разное время; сначала терминал
спрашивает \enquote{можно?}, потом эквайер предъявляет финансовую
запись. Это стандарт для большинства карточных покупок~\cite{visa-core-rules,mc-tpr}.

Существует и другая архитектура: односообщная
(single-message), в которой авторизация и финансовая запись
совмещены в одном сообщении. Такой запрос одновременно спрашивает
разрешение \emph{и} фиксирует финансовое обязательство. Ответ
эмитента в односообщной схеме означает одновременно \enquote{одобрено}
и \enquote{деньги можно считать списанными}: отдельного
клирингового шага не требуется.

\subsection*{Где используется каждая схема}

Двухсообщная схема доминирует в обычных карточных покупках,
в магазинах, ресторанах и во многих интернет-операциях,
потому что допускает расхождение между авторизованной и финальной
суммой. Это важно там, где возможны чаевые, довзвешивание товара
или частичная отмена. Кроме того, эквайер группирует транзакции
в пакеты и обрабатывает их не по одной, а сериями.

Односообщная схема -- классическая модель для банкоматов (ATM):
когда держатель снимает наличные, нет смысла разделять
авторизацию и финансовую запись, потому что деньги уже выданы
физически и отменить операцию задним числом нельзя. По той же
логике односообщная схема используется в некоторых дебетовых
сетях, где транзакция всегда проходит на точную сумму и не
предполагает последующей корректировки~\cite{visa-core-rules,mc-tpr}.

На уровне ISO~8583 различие выражается в MTI (Message Type
Indicator): авторизационный запрос в двухсообщной схеме имеет MTI~0100, а финансовый запрос в односообщной схеме имеет MTI~0200;
соответствующие ответы: MTI~0110 и MTI~0210. Структура MTI подробно
разбирается в следующей главе (\ref{ch:iso8583}).

\importantnote{%
Выбор между двухсообщной и односообщной схемой -- не решение продавца
или разработчика, а свойство канала и правил карточной сети.
POS-терминал, работающий по протоколу Visa или Mastercard,
отправляет двухсообщный поток; банкомат, работающий в сети ATM,
отправляет односообщный. Попытка отправить финансовое сообщение (MTI~0200)
туда, где ожидается авторизационное (MTI~0100), приведёт
к отказу сети.

}
\subsection*{Гибридные варианты}

Граница между двухсообщной и односообщной схемой на практике размывается.
Visa, Mastercard и процессоры поверх них поддерживают
операционные гибриды, в которых одобренная авторизация при
определённых условиях автоматически доводится до финансовой записи
или обслуживается общей инфраструктурой авторизации и клиринга.
Эти гибриды упрощают жизнь эквайерам, но добавляют сложности
процессинговым системам, которым приходится учитывать оба сценария.

\section{Три способа отменить платёж}\label{sec:tx-cancellation}

Отмена карточного платежа -- область, где разрыв
между авторизацией и settlement создаёт наибольшую сложность.
В отличие от банковского перевода, который либо прошёл, либо нет,
карточная транзакция в каждый момент времени находится в одной
из нескольких фаз, и способ отмены зависит от того, в какой фазе
она находится.

\subsection*{Сообщение отмены (reversal)}

Reversal отменяет авторизацию
\emph{до того}, как транзакция ушла в клиринг. С точки зрения
ISO~8583 reversal -- отдельный тип сообщения (MTI~0400),
которое эквайер отправляет в карточную сеть с просьбой
аннулировать ранее одобренную авторизацию.

Reversal возникает в нескольких типовых сценариях: покупатель
передумал у кассы, терминал не получил ответ на авторизацию
(таймаут) и на всякий случай отправляет reversal, кассир ошибся
с суммой и отменяет операцию. При успешном reversal эмитент
снимает блокировку суммы (hold) со счёта держателя,
и средства снова становятся доступными, обычно мгновенно или
в течение нескольких минут. В правилах Mastercard
обязанность сформировать reversal сформулирована вполне жёстко:
эквайер должен обеспечить отправку Reversal Request/0400
в течение 24~часов после отмены ранее авторизованной операции
или после её завершения на меньшую сумму; у Visa
reversal также обязателен, если одобрение пришло уже после таймаута~\cite{visa-core-rules,visa-tadg,mc-tpr}.

Reversal остаётся самым прямым способом отмены: он работает
в реальном времени, не порождает финансовой записи и не требует
возврата денег, потому что деньги ещё не перемещались. Но reversal
возможен только до клиринга; после того как транзакция попала
в клиринговый файл, отменить авторизацию уже нельзя~\cite{mc-rules}.

\subsection*{Аннулирование до расчёта (void)}

Void (аннулирование) -- не отдельный тип сообщения карточной сети,
а операционный термин эквайринга и кассового ПО для отмены
операции до окончательного clearing. Visa прямо описывает
такой сценарий: в dual-message среде cancellation должна происходить
до того, как транзакция уйдёт в clearing на сторону эквайера; если отмена
случилась уже после выхода в онлайн-контур, устройство или продавец
должны сформировать reversal, а саму транзакцию убрать
из пакета clearing или пометить как void~\cite{visa-tadg}.

С точки зрения держателя карты void и reversal выглядят
одинаково: hold снимается, деньги возвращаются на счёт.
Но с технической стороны void может обрабатываться медленнее,
чем reversal, потому что он привязан к циклу клиринга:
если клиринговый файл уже отправлен, корректировка попадёт
только в следующий файл.

\practicenote{%
В API большинства PSP и платёжных шлюзов вы встретите метод
\texttt{cancel} или \texttt{void}, который на самом деле
абстрагирует оба механизма: если транзакция ещё не ушла
в клиринг, PSP отправляет reversal; если ушла, но settlement
не произошёл, применяется void. Для разработчика, интегрирующего платежи,
это удобная абстракция, но внутри неё скрываются разные
процессы с разной скоростью возврата средств клиенту.

}
\subsection*{Возврат после расчёта (refund)}

Refund (возврат) остаётся единственным способом вернуть деньги после того,
как settlement завершён и средства фактически перемещены между
банками. В отличие от reversal и void, которые отменяют исходную
транзакцию, refund -- \emph{новая} транзакция в обратном
направлении: эквайер инициирует перевод от продавца к держателю
карты через ту же карточную сеть.

Refund проходит собственный цикл авторизации, клиринга
и settlement, только в обратную сторону. В Visa TADG
refund описан как онлайн-авторизационное сообщение с последующим
clearing; Mastercard прямо допускает refund как операцию
в dual-message или single-message контуре, а для возвратов,
идущих по dual-message-контуру, требует онлайн-авторизацию~\cite{visa-tadg,mc-tpr}.

Для держателя карты refund остаётся самым медленным вариантом возврата:
средства могут поступить на счёт через несколько рабочих дней,
потому что refund должен пройти полный цикл клиринга и settlement.
Для продавца refund обычно и самый дорогой операционно: помимо
самой суммы возврата, часть расходов исходной покупки и обработки
возврата может не компенсироваться автоматически, а эквайер
нередко берёт отдельную комиссию за такую операцию.

\begin{figure}[tbp]
  \centering
  \includefiguresvg[width=\linewidth]{assets/figures/ch04-transaction-lifecycle/tx-cancellation-timeline}
  \caption{Окна применимости reversal, void и refund на временной
  шкале транзакции.}\label{fig:tx-cancellation-timeline}
\end{figure}

\importantnote{%
Распространённая ошибка при проектировании платёжной интеграции:
использовать refund там, где достаточно reversal или void.
Если клиент отменяет заказ через минуту после оплаты, а система
продавца отправляет refund вместо reversal, клиент будет ждать
возврата несколько дней вместо нескольких минут, продавец потеряет
interchange, а эквайер зафиксирует лишнюю транзакцию. Правило
простое: отменяй как можно раньше: reversal лучше void,
void лучше refund.

}
\section{Граничные случаи}\label{sec:tx-edge-cases}

Схема \enquote{авторизация на точную сумму, клиринг на ту же
сумму, settlement через день} описывает идеальный случай, который
покрывает основную массу розничных покупок. Но платёжная индустрия
полна сценариев, не укладывающихся в эту модель, и каждый из них
требует своего механизма.

\subsection*{Предавторизация (pre-authorization)}

Предавторизация (pre-auth) означает авторизацию,
при которой финальная сумма заранее неизвестна. Классические
примеры: гостиница и аренда автомобиля: в момент заселения или
получения машины продавец авторизует оценочную сумму
(ожидаемую стоимость проживания или депозит за автомобиль),
а итоговая определяется при выезде или возврате и может быть
как меньше, так и больше авторизованной.

С точки зрения сетевых правил pre-auth образует самостоятельный тип запроса.
Visa различает
Estimated Authorization и Incremental Authorization, а
Mastercard рекомендует кодировать запрос как
preauthorization, если сумма оценочная или если операция может
вообще не завершиться. Эмитент устанавливает hold, но уже знает,
что финальный clearing может отличаться от исходной оценки.
Допустимые пределы такого расхождения зависят от MCC, типа
ТСП и конкретного сценария, поэтому точные пороги нужно
брать из актуальных правил сети~\cite{visa-tadg,visa-core-rules,mc-tpr}.

\practicenote{%
Если вы проектируете систему бронирования или маркетплейс услуг
с переменной стоимостью, pre-auth становится вашим основным инструментом.
Но учтите два нюанса: во-первых, hold на карте клиента будет
стоять на полную авторизованную сумму, даже если итоговая покажется
меньше; во-вторых, если финальная сумма окажется значительно
больше авторизованной, вам потребуется либо инкрементальная
авторизация, либо новая транзакция на разницу.

}
\subsection*{Инкрементальная авторизация (incremental authorization)}

Инкрементальная авторизация
увеличивает сумму ранее одобренной авторизации без отмены
исходной. Продавец отправляет дополнительный авторизационный
запрос, ссылающийся на исходный через код авторизации
или специальный идентификатор, и эмитент увеличивает hold
на запрошенную сумму~\cite{visa-tadg,visa-core-rules,mc-tpr}.

Типичный сценарий: гостиница, где гость продлевает пребывание
или заказывает дополнительные услуги: вместо того чтобы
авторизовать и отменять повторно, продавец наращивает сумму
инкрементально. Но поддержка такого потока зависит от схемы,
региона, MCC и настроек эмитента/эквайера, поэтому при
проектировании интеграции нужен и запасной сценарий
(например, новая авторизация на дополнительную сумму).

\subsection*{Частичная авторизация (partial authorization)}

Частичная авторизация означает сценарий,
в котором эмитент одобряет транзакцию не на полную запрошенную
сумму, а лишь на часть. Например, держатель пытается оплатить
покупку на тысячу рублей, но на карте доступно только семьсот.
Вместо полного отказа с кодом \texttt{51} эмитент может одобрить
операцию на семьсот рублей, а продавец предложит клиенту доплатить
оставшиеся триста другим способом: наличными или другой картой~\cite{visa-tadg,mc-tpr}.

Частичная авторизация требует поддержки на всех уровнях:
терминал должен уметь обработать частичное одобрение и предложить
клиенту доплату, POS-система должна скорректировать чек, а кассир должен
понять, что происходит. Чаще всего такая схема встречается
в сценариях с предоплаченными и подарочными картами, где остаток
заранее известен и ограничен.

\importantnote{%
Если ваша система не поддерживает partial authorization, убедитесь,
что терминал или платёжный шлюз не заявляет такую поддержку
в сетевом индикаторе partial approval. Например, у
Mastercard для этого используется Partial Approval
Terminal Support Indicator в DE~48, subelement 61, subfield 1.
Получить частичное одобрение и не обработать его корректно,
это верный путь к расхождениям в учёте: продавец отпустит товар
на полную сумму, а спишется только часть~\cite{mc-tpr}.

}
\subsection*{Отложенная авторизация (deferred authorization)}

Отложенная авторизация означает механизм,
при котором транзакция совершается без онлайн-авторизации,
а авторизационный запрос отправляется позже, когда связь
восстановлена. Классические сценарии: оплата на борту самолёта
(терминал работает офлайн, авторизации отправляются после посадки),
паркинг, где сумма определяется по времени стоянки, платные
дороги и общественный транспорт.

В режиме deferred authorization терминал собирает данные карты,
фиксирует операцию локально и позже формирует авторизационный
запрос с соответствующим флагом, указывающим, что транзакция
уже совершена. Эмитент может одобрить или отклонить этот запрос,
но товар или услуга уже предоставлены, что создаёт кредитный
риск для эквайера и продавца. Поэтому deferred authorization
разрешена только для определённых MCC и при соблюдении лимитов
на сумму отдельной операции, устанавливаемых карточными
сетями. У Visa такие авторизации обычно нужно завершить
в течение 24~часов с момента исходной операции, а в чиповом потоке
они дополнительно маркируются кодом причины \texttt{5206}~\cite{visa-tadg,visa-core-rules}.

\subsection*{Принудительная отправка (force post)}

Принудительная отправка означает механизм, при котором
продавец отправляет транзакцию в клиринг без предварительной
онлайн-авторизации, используя код авторизации, полученный
другим путём, как правило, по телефону через голосовую
авторизацию (voice authorization).

Сценарий типичен для ситуаций, когда онлайн-авторизация невозможна
(терминал не работает, связь пропала), но продавцу всё же нужно
провести платёж: он получает голосовую авторизацию у эмитента
или через эквайера, а затем использует этот код при дальнейшей
обработке транзакции. Mastercard прямо оговаривает, что код авторизации,
полученный по каналу голосовой авторизации, сам по себе не защищает
от chargeback по авторизационным основаниям~\cite{mc-tpr}.

Force post несёт повышенные риски: в таком потоке нет обычной
онлайн-проверки криптограммы, а часть контроля переносится
на человека и процедуры эквайера. Поэтому ответственность
за последствия в значительной степени ложится на продавца.
Force post становится всё более редким: устойчивые каналы связи
и офлайн-режимы терминалов покрывают большинство сценариев, где раньше
требовалась голосовая авторизация. Полностью механизм не исчезает,
но остаётся запасным вариантом.

\begin{table}[tbp]
  \begingroup
  \small
  \setlength{\tabcolsep}{4pt}
  \renewcommand{\arraystretch}{1.2}
  \makebox[\textwidth][l]{%
  \begin{tabularx}{\fullwidth}{
      @{}>{\RaggedRight\arraybackslash\bfseries}p{0.14\textwidth}
      >{\RaggedRight\arraybackslash}p{0.17\textwidth}
      >{\RaggedRight\arraybackslash}p{0.15\textwidth}
      >{\RaggedRight\arraybackslash}p{0.17\textwidth}
      Y@{}
    }
    \toprule
    \rowcolor{Panel}
    \textbf{Механизм} & \textbf{Когда сумма известна} &
    \textbf{Онлайн-авторизация} & \textbf{Кто несёт риск} &
    \textbf{Типичный MCC} \\
    \midrule
    \rowcolor{Soft}
    Pre-auth & позже & да
    & эквайер/ТСП & 7011 (гостиницы), 7512 (аренда авто) \\
    Incremental auth & постепенно & да (несколько)
    & эквайер/ТСП & 7011 \\
    \rowcolor{Soft}
    Partial auth & сразу, но покрытие неполное & да
    & ТСП & любой (чаще prepaid) \\
    Deferred auth & позже & нет (офлайн)
    & эквайер/ТСП & 4511 (авиа), 7523 (паркинг) \\
    \rowcolor{Soft}
    Force post & сразу & голосовая
    & ТСП & любой \\
    \bottomrule
  \end{tabularx}%
  }
  \endgroup
  \caption{Граничные случаи карточной авторизации.}\label{fig:tx-edge-cases}
\end{table}

