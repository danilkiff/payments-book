% /Users/pikachu/books/payments-book/src/parts/part2/ch11-3ds.tex
\chapter{3-D Secure: кто отвечает за фрод}
\label{ch:3ds}
\margintoc


\begin{kaobox}[title=Что вы узнаете из этой главы]
  EMV-чип (глава \ref{ch:emv}) резко сократил подделку физической
  карты, но риск сместился в канал без физического предъявления карты.
  3-D Secure — это ответ индустрии на такие транзакции: протокол,
  который вводит дополнительный слой аутентификации и перераспределяет
  ответственность за мошенничество. Мы разберём три домена протокола,
  переход от первой версии ко второй, аутентификацию на основе риска,
  перенос ответственности, освобождения от SCA в Европе и связь
  токенизации с 3DS.
\end{kaobox}

Представим обычное оформление заказа в интернет-магазине. Покупатель
вводит PAN, срок действия и CVV, нажимает \enquote{оплатить}, а мерчант
должен решить две разные задачи сразу: понять, можно ли отправлять
транзакцию на авторизацию, и не разрушить сценарий покупки лишним
трением. Вся глава отвечает на один вопрос: каким образом индустрия
добавила в операции без физического предъявления карты
(card-not-present, CNP)\sidenote{CNP (card-not-present) — операция,
в которой реквизиты карты
передаются удалённо.} недостающий слой аутентификации, не превратив
каждую интернет-покупку в отдельное испытание.

\section{CNP-фрод как причина появления 3DS}
\label{sec:3ds-cnp-fraud}

В базовой CNP-модели мерчант располагает лишь карточными реквизитами:
номером карты, сроком действия, CVV2 и, возможно, адресом или
почтовым индексом. Этих данных достаточно, чтобы сформировать
авторизационный запрос, но недостаточно, чтобы убедительно доказать,
что покупку инициирует именно законный держатель. Реквизиты можно
подсмотреть, сфотографировать, выманить фишингом или получить из
утечки — и после этого злоумышленник оказывается в почти той же
позиции, что и реальный клиент.

По мере того как EMV-чип сделал клонирование физической карты
намного менее выгодным, давление сместилось в дистанционный канал.
Для европейского рынка это хорошо видно по данным ЕЦБ: в отчёте по
SEPA CNP-фрод составлял примерно 84\% от общей стоимости карточного
мошенничества и в 2020, и в 2021 году, а сам сдвиг ЕЦБ связывает
с ростом электронной коммерции и тем, что защита канала
с физическим предъявлением карты
стала заметно сильнее \sidecite{ecb-card-fraud-2021}.

Карточным сетям нужен был механизм, который решал бы для CNP-транзакций
ту же задачу, что чип решает в терминале: добавить независимый
сигнал о том, что транзакцию инициировал законный держатель.
Этим механизмом стал 3-D Secure — семейство протоколов, которое
рынок сначала узнал по брендам \enquote{Verified by Visa},
\enquote{Mastercard SecureCode} и аналогичным программам других
сетей, а затем — как единый стандарт под управлением EMVCo
\sidecite{emvco-3ds}.

Итак, 3-D Secure появился не как техническая прихоть карточных сетей,
а как ответ на структурную проблему CNP-платежей: карточных
реквизитов недостаточно, чтобы убедительно связать транзакцию
с держателем. Следующий шаг — понять, кто именно в этой
модели собирает данные, кто принимает решение и кто обеспечивает
совместимость между ними.

\section{Три домена, три участника}
\label{sec:3ds-three-domains}

Название протокола — 3-D Secure — буквально означает
\enquote{безопасность в трёх доменах}. Каждый домен объединяет
участников, выполняющих определённую роль:

\begin{enumerate}
  \item \textbf{Домен эмитента} (Issuer Domain) — отвечает
        за аутентификацию держателя карты. Ключевой компонент —
        ACS (Access Control Server), сервер эмитента или его
        процессора, который решает, достаточно ли данных для
        незаметной аутентификации или нужно запросить явное
        подтверждение у держателя.
  \item \textbf{Домен эквайера} (Acquirer Domain) — отвечает
        за инициирование аутентификации. Ключевой компонент —
        сервер 3DS (3DS Server), встроенный в инфраструктуру
        мерчанта или его платёжного провайдера, который собирает контекст транзакции
        и отправляет запрос.
  \item \textbf{Домен совместимости} (Interoperability Domain) —
        обеспечивает маршрутизацию между первыми двумя. Ключевой
        компонент — DS (Directory Server), сервер карточной сети,
        который по данным карты определяет нужный ACS и доставляет
        сообщения между сторонами.
\end{enumerate}

\begin{kaobox}[title={Определение: ACS}, colback=blue!4, colbacktitle=blue!15]
  ACS (Access Control Server) — сервер в домене эмитента, принимающий
  решение об аутентификации держателя. ACS оценивает риск транзакции,
  выбирает между незаметным сценарием и сценарием с дополнительным
  подтверждением и формирует результат
  аутентификации (authentication value), который передаётся
  в авторизационном сообщении.
\end{kaobox}

\begin{kaobox}[title={Определение: DS}, colback=blue!4, colbacktitle=blue!15]
  DS (Directory Server) — сервер в домене совместимости, управляемый
  карточной сетью. DS принимает Authentication Request (AReq) от сервера 3DS
  мерчанта, по BIN карты определяет ACS эмитента и маршрутизирует
  сообщение к нему, а затем возвращает Authentication Response (ARes)
  обратно. DS не принимает решение об аутентификации — это целиком
  задача ACS; роль DS ограничена маршрутизацией и обеспечением
  взаимодействия между доменами.
\end{kaobox}

\begin{figure}
  \centering
  \includegraphics[width=\linewidth]{assets/figures/ch11-3ds/3ds-three-domains.pdf}
  \caption{Архитектура трёх доменов 3-D Secure.}
  \label{fig:3ds-three-domains}
\end{figure}

Важно зафиксировать инвариант этой схемы: мерчант и его сервер 3DS
собирают контекст, DS маршрутизирует сообщения, а решение принимает
ACS эмитента. Дальнейшая эволюция 3DS меняла прежде всего то,
\emph{как} передаётся контекст и \emph{как} выглядит подтверждение
для держателя, но не это разделение ролей.

\section{3DS v1 — редирект и его проблемы}
\label{sec:3ds-v1}

Первая версия протокола работала через браузерный редирект. При оплате
на сайте мерчанта браузер
перенаправлялся на страницу ACS эмитента, часто во всплывающем
окне или внутри встроенного фрейма, где держатель вводил статический
пароль — тот самый \enquote{Verified by Visa} или \enquote{SecureCode},
который эмитент просил задать при активации сервиса.

Проблемы 3DS v1 были не случайными сбоями, а следствием самой
конструкции. Их удобно разложить на три группы: издержки
пользовательского сценария, технические ограничения и архитектурную
негибкость.

С точки зрения пользователя статический пароль был главным
источником трения. Держатели карт регулярно забывали пароль,
которым пользовались раз в несколько месяцев; путали его с PIN-кодом
или паролем от интернет-банка; не могли отличить настоящую страницу ACS
от фишинговой подделки, поскольку обе выглядели одинаково —
всплывающее окно с незнакомым доменом. Восстановление пароля
требовало звонка в банк или отдельной процедуры подтверждения,
и значительная часть держателей на каждом из этих шагов просто
бросала покупку.

Технические проблемы усугублялись по мере перехода пользователей
на мобильные устройства. Протокол разрабатывался в эпоху десктопных
браузеров: редирект на стороннюю страницу в середине оформления
заказа ломал пользовательский сценарий; всплывающие окна
блокировались браузером; встроенные фреймы нестабильно работали
на мобильных устройствах и не годились для нативных приложений.
Насколько сильно это било по конверсии, зависело от мерчанта,
но сама конструкция системно увеличивала число незавершённых
покупок.

Архитектурная проблема состояла в бинарности решения: ACS
в 3DS v1 мог лишь пропустить транзакцию без пароля —
фактически игнорируя аутентификацию — или запросить
статический пароль. Возможности оценить риск транзакции
и принять взвешенное решение не было: не существовало ни понятия
\enquote{уровень риска}, ни механизма получить богатый набор сведений
о контексте покупки, ни способа инициировать подтверждение через
мобильное банковское приложение.

Несмотря на это, эмитенты активно поддерживали 3DS v1, потому что
протокол обеспечивал перенос ответственности (liability shift): если мерчант использовал
3DS и аутентификация была одобрена, ответственность за мошенничество
переходила на эмитента, а мерчант получал защиту от чарджбэков
по причине фрода. Для мерчантов этот обмен — потеря конверсии
в обмен на снижение чарджбэков — оказывался выгодным далеко
не всегда: высокорисковые категории (электроника, товары класса люкс)
выигрывали от защиты, тогда как низкорисковые
(цифровые товары, небольшие суммы)
несли чистые потери выручки \sidecite{emvco-3ds}.

Итак, первая версия решила экономическую задачу мерчанта, но
плохо решила пользовательскую задачу покупателя. Именно поэтому
следующий шаг в развитии протокола состоял не просто в усилении
контроля, а в попытке сделать его выборочным и контекстным.

\section{3DS v2 — аутентификация на основе риска}
\label{sec:3ds-v2}

Вторая версия 3DS была спроектирована как полноценная замена
первой: протокол сохранил идею дополнительной аутентификации,
но отказался от обязательного редиректа и статического пароля.
EMVCo развивает это семейство спецификаций как набор версий 2.x
\sidecite{emvco-3ds}. Главное нововведение — аутентификация
на основе риска (Risk-Based Authentication, RBA)\sidenote{RBA
(Risk-Based Authentication) — аутентификация, в которой решение
принимается по совокупности контекстных
сигналов о транзакции и держателе.}: ACS больше не делает
жёсткий выбор \enquote{пароль или ничего}, а оценивает риск и
выбирает подходящий сценарий.

\subsection*{Незаметный сценарий (frictionless flow)}

Если ACS считает транзакцию низкорисковой, аутентификация
происходит \emph{невидимо} для держателя: сервер 3DS отправляет
данные в ACS, ACS анализирует их, возвращает положительный результат,
и транзакция уходит на авторизацию — держатель ничего не замечает.
Именно этот режим сделал 3DS пригодным для массовой электронной
коммерции: дополнительная проверка происходит, но не ломает
оформление заказа.

Решение ACS опирается на контекст, который сервер 3DS передаёт
вместе с запросом: сведения об устройстве, браузере или приложении,
адресе доставки, истории покупок у данного мерчанта, характере
самой операции и других признаках. Чем богаче и чище этот контекст,
тем точнее оценка риска и тем чаще эмитент может пропустить
операцию без явного участия держателя.

\subsection*{Сценарий дополнительного подтверждения (challenge flow)}

Если ACS оценивает транзакцию как рискованную — новое устройство,
необычная сумма, нетипичная страна, новый мерчант — он инициирует
запрос дополнительного подтверждения у держателя. В отличие от
первой версии, речь идёт уже о более
гибком наборе методов. ACS инициирует Challenge Request (CReq)
и передаёт его через DS в браузер или 3DS SDK; держатель
подтверждает операцию, после чего Challenge Response (CRes)
возвращается к ACS \sidecite{emvco-3ds}.

Набор доступных методов подтверждения существенно шире, чем в 3DS v1.
Это может быть одноразовый пароль, push-уведомление в мобильном
банке, подтверждение в банковском приложении с биометрией или
внеполосное подтверждение на сопряжённом устройстве. С инженерной точки
зрения здесь важен сдвиг модели:
аутентификация стала нативной для мобильной среды и перестала
зависеть от отдельного пароля, который держатель почти наверняка
успеет забыть.

Практический эффект второй версии состоит в другом ритме решения.
Большая часть транзакций может пройти незаметно для держателя,
а те операции, для которых подтверждение всё-таки нужно, проходят
в интерфейсе, больше похожем на обычный мобильный банк, чем на
чужую страницу с паролем. Для ACS наиболее ценны те сигналы,
которые помогают отличить привычную покупку от нетипичной:
характер устройства, история у мерчанта, способ доставки,
повторяемость поведения и иные контекстные признаки
\sidecite{emvco-3ds}.

Итак, инвариант v2 таков: решение по-прежнему принимает эмитент,
но теперь он делает это на гораздо более богатом наборе сигналов.
За счёт этого 3DS перестаёт быть почти постоянной помехой и
становится выборочным механизмом проверки.

\begin{figure}
  \centering
  \includegraphics[width=\linewidth]{assets/figures/ch11-3ds/3ds-v1-vs-v2.pdf}
  \caption{Сравнение 3DS v1 и 3DS v2.}
  \label{fig:3ds-v1-vs-v2}
\end{figure}

\section{Асинхронная аутентификация}
\label{sec:3ds-decoupled}

3DS v2 ввёл ещё один сценарий: асинхронную аутентификацию
(decoupled authentication), то есть подтверждение, которое происходит
\emph{не в момент} покупки и \emph{не в контексте} страницы оплаты.
Зачем экосистеме понадобился такой механизм? Обычный сценарий
дополнительного подтверждения предполагает, что держатель находится
рядом с устройством и может немедленно ответить на запрос. Однако
существует целый класс ситуаций,
где это условие не выполняется: устройство интернета вещей без экрана
(умная колонка, подключённый автомобиль) инициирует покупку
без участия человека; рекуррентная транзакция запускается
ночью по расписанию; SDK эмитента несовместим с платформой
мерчанта и сценарий подтверждения технически невозможен
в текущей сессии. Для всех этих случаев асинхронная аутентификация даёт рабочий
выход \sidecite{emvco-3ds}.

Технически ACS возвращает статус \enquote{D} (decoupled) в ARes.
Мерчант получает этот ответ, переводит заказ в состояние ожидания,
а покупатель видит сообщение вроде \enquote{заказ принят, ожидаем подтверждения}.

Дальше сервер 3DS либо периодически запрашивает итоговый статус,
либо получает его по предусмотренному интеграцией асинхронному
каналу. Параллельно эмитент связывается с держателем доступным
способом: через push-уведомление, SMS или звонок оператора.

Спецификация задаёт максимальное время ожидания
(decoupled max timeout), после которого аутентификация считается
неудавшейся. Если она не завершена вовремя, мерчант либо отменяет
заказ, либо отправляет транзакцию без результата аутентификации,
принимая повышенный риск \sidecite{emvco-3ds}.

Для пользовательского сценария асинхронная аутентификация требует
отдельного проектирования. Типичный вариант: \enquote{Мы отправили
  вам уведомление в приложении — подтвердите, пожалуйста,
  покупку, и мы отгрузим заказ}. Это условное подтверждение
заказа вместо немедленного — сценарий, требующий специальной
обработки в системе управления заказами (отдельный статус,
таймаут, уведомление при подтверждении или отказе) и понятного
объяснения держателю, чтобы он не забыл нажать кнопку
в банковском приложении \sidecite{emvco-3ds}.

Иначе говоря, асинхронная аутентификация полезна там, где момент
оформления заказа и момент подтверждения можно развести без потери
смысла для покупателя и без операционного хаоса для мерчанта.

\section{Перенос ответственности: когда он работает}
\label{sec:3ds-liability}

Перенос ответственности (liability shift) — это договорное
перераспределение риска мошеннического чарджбэка с мерчанта
и эквайера на эмитента.
Именно он остаётся главным экономическим стимулом для внедрения
3DS. Однако этот перенос никогда не бывает автоматическим:
он зависит от результата аутентификации, статуса операции,
правил конкретной сети и, в европейском контуре, от того,
использовалось ли освобождение от SCA \sidecite{visa-core-rules}.

\begin{kaobox}[title=Важно, colback=red!5, colbacktitle=red!20]
  Четыре сценария, которые необходимо различать:

  \begin{enumerate}
    \item \textbf{3DS выполнен, эмитент одобрил (незаметный сценарий
            или подтверждение пройдено).} Перенос ответственности
          обычно работает: риск уходит к эмитенту.
          Мерчант защищён от чарджбэка по коду причины,
          связанному с мошенничеством.
    \item \textbf{Попытка аутентификации выполнена, но ACS недоступен.}
          В ряде программ карточных сетей попытка аутентификации
          (\enquote{attempted authentication}) всё ещё даёт защиту
          мерчанту, но это правило всегда нужно сверять с текущими
          документами сети.
    \item \textbf{Мерчант не использует 3DS.} Перенос ответственности
          не работает: мерчант и эквайер несут ответственность
          за мошенничество.
    \item \textbf{Мерчант использует освобождение} (см.\ следующий раздел).
          Перенос ответственности, как правило, \emph{не работает}:
          мерчант и эквайер
          осознанно отказались от аутентификации и приняли риск
          на себя.
  \end{enumerate}
\end{kaobox}

Правила различаются по сетям и регионам. У \scheme{Visa}
и \scheme{Mastercard} есть собственные нюансы для попытки
аутентификации, рекуррентных операций и отдельных категорий
мерчантов.

В России, где PSD2 не действует, перенос ответственности определяется
правилами НСПК для карт Мир и правилами международных сетей
для \scheme{Visa} и \scheme{Mastercard}.
\todo[inline]{Проверить и при необходимости добавить источник:
  точную матрицу переноса ответственности по 3DS для карт Мир
  нужно сверять по актуальным правилам НСПК для
  участников системы; в открытых публичных материалах НСПК это
  обычно описано только на общем уровне.}
Поэтому в российском контуре 3DS нужно читать не как универсальную
гарантию защиты, а как условный механизм, который работает только
в пределах правил конкретной сети.

\section{Освобождения PSD2 и российская специфика}
\label{sec:3ds-exemptions}

В Европейском союзе директива PSD2 и технические стандарты EBA
требуют сильной клиентской аутентификации
(Strong Customer Authentication, SCA) для всех электронных
платежей, за исключением ряда освобождений
\sidecite{psd2}. По существу речь идёт о проверке как минимум
по двум из трёх факторов: знание (пароль, PIN), владение
(телефон, карта) и биометрия (отпечаток, лицо)
\sidecite{eba-rts-sca}.

Для 3-D Secure важны не юридические детали освобождений сами по себе,
а их влияние на решение эмитента. Освобождение — это попытка пройти
по упрощённому пути, без дополнительного подтверждения:
мерчант или эквайер просит
обойтись без полного SCA, но окончательное решение всё равно
остаётся за эмитентом. На практике это означает три вещи.

Во-первых, освобождения не отменяют риск-модель эмитента:
\enquote{low value}, \enquote{trusted beneficiary}, \enquote{recurring}
и TRA работают только
пока эмитент считает транзакцию допустимой. Во-вторых,
использование освобождения обычно ослабляет или вовсе выключает
перенос ответственности: мерчант просит меньше трения, но вместе
с этим чаще принимает на себя больший риск мошенничества.
В-третьих, 3DS v2 остаётся важным даже при освобождении, потому
что именно через него
передаются индикаторы риска, запрошенные послабления и решение
эмитента по транзакции \sidecite{eba-rts-sca}.

\begin{kaobox}[title={Основные освобождения PSD2 (справка)}]
  \begin{itemize}
    \item \textbf{Low value} — дистанционная операция до 30~EUR,
          пока не превышены кумулятивные лимиты.
    \item \textbf{TRA} — освобождение по анализу риска
          (Transaction Risk Analysis) для сумм до порогов,
          установленных RTS.
    \item \textbf{Trusted beneficiary} — мерчант в белом списке держателя.
    \item \textbf{Recurring} — первая операция проходит SCA, последующие
          могут быть освобождены.
    \item \textbf{Secure corporate} — отдельные корпоративные платёжные сценарии.
  \end{itemize}
\end{kaobox}

\subsection*{Российская специфика}

Россия не входит в зону действия PSD2, и российское законодательство
не содержит прямого аналога требования SCA. Решение
о необходимости аутентификации по 3DS для внутрироссийских транзакций
принимается эмитентом на основании его собственной политики
и правил карточной сети. На практике российские банки активно
используют 3DS v2 с аутентификацией на основе риска: незаметный
сценарий для типичных операций и дополнительное подтверждение
для подозрительных. Более широкий разговор о том, как PSD2 и SCA меняют
модель доступа к счёту, вынесен в главу \ref{ch:open-banking},
где этот сюжет уже читается не как частный 3DS-механизм,
а как часть счётовых платёжных схем.

\section{3DS и токенизация: как работают вместе}
\label{sec:3ds-tokenization}

Токенизация (глава \ref{ch:tokenization}) и 3DS решают разные задачи.
Токен отвечает на вопрос, какими именно карточными реквизитами
мы пользуемся в этой операции, а 3DS — на вопрос, действительно ли
эту операцию инициирует законный держатель. Вместе эти два слоя
дают эмитенту более полную картину риска. Эта связка проявляет
себя по-разному в разных сценариях.

При провизионировании мобильного кошелька поставщик токенов
(Token Service Provider, TSP) может инициировать дополнительную
аутентификацию через 3DS ещё до того, как токен будет выпущен.
Держатель добавляет карту в Apple Pay или Google Pay — и вместо
того чтобы поставщик токенов оценивал риск лишь по данным карты и устройства,
эмитент запрашивает прохождение сценария дополнительного
подтверждения 3DS. Успешное подтверждение
служит подтверждением, что карту добавил именно законный держатель,
а не тот, кто нашёл реквизиты в утечке. В результате выпущенный
токен получает высокий уровень доверия, и все последующие
транзакции с этим кошельком будут одобряться
с более высокой вероятностью \sidecite{emvco-3ds}.

При электронной операции с ранее сохранённой картой
(card-on-file) мерчант включает
в авторизационное сообщение одновременно токен карты (DPAN) и значение
аутентификации по 3DS. Эмитент получает два независимых подтверждения:
токен с уровнем доверия говорит о том, что карточные реквизиты
были проверены при провизионировании,
а CAVV или AAV — что держатель аутентифицировался непосредственно
перед этой покупкой. Ни один из этих слоёв по отдельности не даёт
той же уверенности, что оба вместе \sidecite{emvco-3ds}.

Для рекуррентных подписок логика иная. Первая клиентская транзакция
(CIT, Customer-Initiated Transaction) проходит полный 3DS
с дополнительным подтверждением или незаметным сценарием; после
этого мерчант и эквайер сохраняют ссылку на первую аутентифицированную
операцию, например Network Transaction ID. Все последующие платежи
по подписке — MIT (Merchant-Initiated Transactions) — отправляются
с сетевым токеном и ссылкой на эту первую транзакцию,
тогда как 3DS не требуется: держатель дал согласие однократно,
и это согласие привязано к токену. Если мерчант меняет условия
подписки, новая клиентская инициированная операция обычно должна
снова пройти через аутентификацию \sidecite{emvco-3ds}.

В авторизационном \iso{8583}-сообщении 3DS-данные и данные
токенизации передаются параллельно. Точное размещение полей
зависит от карточной сети и конкретного процессингового диалекта:
какие-то признаки живут в стандартных полях, какие-то — в сетевых
расширениях. Важно то, что эмитент получает и идентификатор токена,
и результат аутентификации в одном и том же авторизационном
сообщении. Подробно о влиянии
токенизации и 3DS на антифрод — в главе \ref{ch:fraud}.

Итак, токенизация снижает риск компрометации реквизитов, а 3DS
снижает риск неправомерного использования уже выпущенного
инструмента. Вместе они дают эмитенту более надёжную модель
доверия, чем каждый механизм по отдельности.

\begin{chaptersummary}
\begin{itemize}
  \item 3-D Secure возник как ответ на риск в канале CNP:
        одних карточных реквизитов для убедительной аутентификации
        держателя недостаточно.
  \item Протокол работает в трёх доменах: эмитентском
        (ACS), эквайерном (сервер 3DS) и домене совместимости
        (DS карточной сети).
  \item Первая версия 3DS опиралась на редирект и статический
        пароль; вторая версия перенесла решение в модель
        аутентификации на основе риска и сделала незаметный
        сценарий нормой для низкорисковых операций.
  \item Перенос ответственности зависит не от самого факта
        наличия 3DS, а от результата аутентификации, статуса
        операции и правил конкретной сети.
  \item В Европе 3DS тесно связан с SCA и освобождениями
        по PSD2; в России обязательность 3DS определяется
        политикой эмитента и правилами карточной сети.
  \item Токенизация и 3DS усиливают друг друга: токен
        помогает идентифицировать платёжный инструмент, а 3DS
        добавляет отдельный сигнал о личности держателя.
\end{itemize}
\end{chaptersummary}
