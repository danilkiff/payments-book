
\chapter{PCI DSS: минимизация того, что нужно защищать}
\label{ch:pci-dss}
\margintoc


\begin{kaobox}[title=Что вы узнаете из этой главы]
  PCI DSS — дисциплина
  проектирования вокруг одной идеи: минимизировать количество мест,
  где вообще появляются карточные данные, и жёстко защитить
  оставшиеся. Здесь мы разберём логику двенадцати требований,
  научимся очерчивать CDE, увидим, как токенизация, P2PE
  и вынесенные платёжные формы сокращают периметр оценки,
  разберём наиболее болезненные требования PCI DSS 4.0.1
  и сопоставим их с актуальными российскими требованиями
  \law{821-П} и \law{851-П}.
\end{kaobox}

Если убрать из PCI язык аудита и контролей, останется один
архитектурный вопрос: где именно в системе появляются данные карты
и сколько компонентов действительно должны их видеть. Всё остальное
следует из ответа на него. Поэтому дальше будем читать PCI DSS
не как бесконечный перечень требований, а как задачу о границах
и об их защите.

\section{Логика двенадцати требований}
\label{sec:pci-logic}

PCI DSS (Payment Card Industry Data Security Standard) определяет
требования к защите данных платёжных карт для любой организации,
которая хранит, обрабатывает или передаёт данные держателя карты
(cardholder data, CHD). Стандарт разрабатывает и поддерживает
PCI SSC (Security Standards Council), созданный глобальными
карточными сетями \sidecite{pci-dss-4}.

До появления PCI DSS у карточных сетей были собственные программы
защиты данных. FAQ PCI SSC прямо указывает, что общий стандарт
изначально возник из выравнивания программ \scheme{Visa} AIS/CISP
и \scheme{Mastercard} SDP \sidecite{pci-faq-1199}. Для мерчанта,
принимающего карты нескольких брендов, это означало переход
от нескольких частично несовпадающих программ к общей базовой
планке безопасности.

Версия 1.1, разработанная учредителями PCI SSC, вступила в силу
вместе с запуском самого совета, а PCI SSC был создан в 2006 году
American Express, Discover, JCB, Mastercard и Visa
\sidecite{pci-faq-1199}\sidecite{pci-faq-1227}. Громкие утечки
вроде TJX и Heartland показали, что вопрос не академический:
компрометация затрагивает не только отдельные магазины, но и крупных
процессоров \sidecite{doj-tjx-2009}\sidecite{doj-heartland-2009}.

Двенадцать требований PCI DSS не являются произвольным списком —
они организованы в шесть групп, каждая из которых защищает
определённый слой:

\begin{enumerate}
  \item \textbf{Безопасная сеть и системы} (Req 1--2).
        Требование 1: установить и поддерживать сетевые средства
        защиты (межсетевые экраны, сегментация). Требование 2:
        применять безопасные конфигурации ко всем компонентам
        (не использовать заводские пароли, отключать неиспользуемые
        сервисы).
  \item \textbf{Защита данных карт} (Req 3--4).
        Требование 3: защитить хранимые данные (шифрование PAN,
        запрет хранения SAD — как мы разбирали
        в главе \ref{ch:card-data}). Требование 4: шифровать
        передачу данных держателя карты по открытым сетям.
  \item \textbf{Управление уязвимостями} (Req 5--6).
        Требование 5: защитить все системы от вредоносного ПО.
        Требование 6: разрабатывать и поддерживать безопасные
        системы и приложения (безопасный жизненный цикл разработки,
        управление патчами,
        защита веб-приложений).
  \item \textbf{Контроль доступа} (Req 7--9).
        Требование 7: ограничить доступ к данным карт по принципу
        необходимого знания. Требование 8: идентифицировать
        пользователей и аутентифицировать доступ (MFA). Требование 9: ограничить
        физический доступ к данным карт.
  \item \textbf{Мониторинг и тестирование} (Req 10--11).
        Требование 10: журналировать и мониторить все обращения
        к сетевым ресурсам и данным карт. Требование 11:
        регулярно тестировать безопасность (сканы уязвимостей,
        тесты на проникновение).
  \item \textbf{Политика информационной безопасности} (Req 12).
        Поддерживать политику, охватывающую все аспекты
        информационной безопасности для всего персонала.
\end{enumerate}

Главная мысль здесь проста: требования образуют концентрическую
оборону, а не механический чеклист. Если данные карты не попадают
в среду мерчанта благодаря токенизации, P2PE или полностью
вынесенной платёжной странице, то большая часть требований
на эту среду просто не распространяется. Лучший способ соответствовать
стандарту — не героически защищать всё подряд, а сначала сузить
область, где вообще есть что защищать. Поэтому следующий шаг —
аккуратно определить границы CDE.

\section{CDE: как определить границы}
\label{sec:pci-cde}

\begin{kaobox}[title={Определение: CDE}, colback=blue!4, colbacktitle=blue!15]
  CDE (Cardholder Data Environment) — совокупность людей, процессов
  и технологий, которые хранят, обрабатывают или передают данные
  держателя карты (CHD) или чувствительные аутентификационные
  данные (sensitive authentication data, SAD), а также
  все системы и сегменты сети, напрямую подключённые к ним
  или влияющие на их безопасность.
\end{kaobox}

Определить границы CDE\sidenote{CDE (Cardholder Data Environment) —
среда, в которой присутствуют данные держателя карты или системы,
напрямую влияющие на их безопасность.} — едва ли не самая трудная
практическая задача в работе по PCI DSS. Ошибка в определении
периметра оценки означает либо недозащищённость, когда данные карты
оказываются за пределами защитного контура, либо избыточность,
когда организация тратит силы на системы, которые карточных данных
никогда не видят.

Обычно в CDE попадают три категории систем \sidecite{pci-scoping-guidance}:

\begin{itemize}
  \item \textbf{Системы, обрабатывающие CHD/SAD} — серверы
        авторизации, базы данных с PAN, терминалы — очевидно
        входят в периметр.
  \item \textbf{Непосредственно связанные системы} (connected-to systems) — системы, напрямую
        подключённые к CDE по сети, даже если они сами не работают
        с карточными данными (например, DNS-сервер в одном сегменте
        с платёжным сервером).
  \item \textbf{Системы, влияющие на безопасность} (security-impacting systems) — системы, влияющие
        на безопасность CDE: серверы аутентификации, системы
        мониторинга, антивирусное ПО, NTP-серверы.
\end{itemize}

Отсюда следует важный практический вывод. Сетевая сегментация —
не косметическая мера и не удобство для аудитора, а главный способ
удержать CDE в разумных границах. Если карточные данные живут
в изолированном сегменте, отделённом от остальной инфраструктуры
межсетевым экраном с политикой запрета по умолчанию, в периметр
попадает именно этот сегмент и его ближайшие зависимости. Если
сегментации нет, частью CDE приходится считать почти всю сеть
организации.

\begin{figure}
  \centering
  \includegraphics[width=\linewidth]{assets/figures/ch12-pci-dss/pci-cde-boundary.pdf}
  \caption{Границы CDE и смежных систем.}
  \label{fig:pci-cde-boundary}
\end{figure}

\section{Сужение периметра: токенизация, P2PE и вынесенные формы}
\label{sec:pci-scope-reduction}

Если CDE описывает ядро среды, то сужение периметра оценки показывает,
как уменьшить число систем, на которые стандарт действительно
распространяется. Ниже четыре типовых механизма: от частичного
сокращения периметра до почти полного вывода мерчанта из CDE.

\subsection*{Токенизация}

Токенизация как способ сузить периметр существует как минимум в двух
разных режимах, и их важно не смешивать. Сетевые токены, подробно
разобранные в главе \ref{ch:tokenization}, — это DPAN, который
выпускает карточная сеть. Они снижают ценность утечки и позволяют
перестроить архитектуру так, чтобы настоящий PAN не ходил
по системам мерчанта. Но эффект легко переоценить: сама по себе
подмена PAN токеном ещё не выводит систему из PCI-периметра.
Вопрос всегда в том, может ли среда увидеть PAN, использовать токен
как полноценный платёжный идентификатор или повлиять на безопасность
тех систем, где PAN всё же существует \sidecite{pci-dss-4}.

PSP-токенизация устроена иначе. Мерчант один раз передаёт PAN
в платёжный шлюз, шлюз хранит его в собственном хранилище токенов
(vault) и возвращает суррогатное значение (payment token). Дальше
мерчант работает только с этим суррогатом. Его периметр действительно
может заметно сузиться, но не исчезает автоматически: шлюз,
владеющий vault, остаётся полноценным субъектом PCI DSS, а точка
первичного ввода PAN требует отдельной защиты \sidecite{pci-dss-4}.

\subsection*{P2PE (Point-to-Point Encryption)}

P2PE — стандарт PCI, при котором данные карты шифруются прямо
в терминале в момент считывания и расшифровываются только в HSM
на стороне эквайера или процессора. Ключевой элемент решения —
SRED-модуль (Secure Reading and Exchange of Data) внутри терминала.
Это аппаратно защищённый компонент, который перехватывает считанные
данные ещё до их попадания в общую память терминала и сразу шифрует
их на ключе, введённом во время процедуры key injection. Поэтому
через инфраструктуру мерчанта проходит не PAN, а только
зашифрованный блок данных \sidecite{pci-p2pe}.

Модель P2PE разделяет ответственность между двумя ролями: P2PE
Component Provider, то есть поставщиком отдельных компонентов
(терминала, HSM, системы управления ключами), и P2PE Solution
Provider, то есть поставщиком сертифицированного конечного решения.
Мерчант, использующий готовое решение P2PE, как правило, получает
существенно меньше применимых требований и может валидироваться
по более узкому сценарию, если это допускают условия его платёжной
программы и требования бренда карты. Точкой расшифровки остаётся
HSM процессора: именно эта среда сохраняет полный PCI DSS-периметр
и несёт ответственность за ключевую защиту \sidecite{pci-p2pe}.

\subsection*{Вынесенные поля оплаты и iFrame}

В электронной коммерции мерчант может вообще не собирать данные
карты на собственной странице. Вместо этого он встраивает iFrame
или вынесенные поля оплаты (hosted fields), которые отрисовывает PSP.
Ключевая граница здесь проходит по принадлежности элемента DOM:
поле ввода живёт в домене PSP, нажатия клавиш уходят прямо туда,
и ни PAN, ни CVV не касаются ни кода JavaScript мерчанта, ни его
серверов. От PSP мерчант получает уже платёжный токен и дальше
работает только с ним \sidecite{pci-dss-4}.

Здесь принципиальна граница ответственности. Если \emph{все} элементы
платёжной страницы поставляются только PCI DSS-совместимым
провайдером, мерчант может документировать соответствие через SAQ A.
Если хотя бы часть элементов формируется на стороне мерчанта, речь
уже идёт о SAQ A-EP. Для сценария со встроенной формой в 2025 году
появилось ещё одно уточнение: SAQ A r1 требует подтвердить, что
страница мерчанта не подвержена атакам через скрипты; это условие
относится именно к встроенной платёжной форме и не распространяется
на чистый redirect или полностью вынесенную оплату
\sidecite{pci-faq-1291}\sidecite{pci-faq-1293}\sidecite{pci-faq-1588}.

\subsection*{Полное перенаправление на страницу PSP}

Полное перенаправление (redirect) — самый радикальный архитектурный
способ вывести мерчанта из CDE. Во время оплаты держатель карты
целиком покидает сайт мерчанта: браузер уходит на страницу
платёжного шлюза, карточные данные вводятся и обрабатываются
\emph{целиком на стороне PSP}, и только после завершения держатель
возвращается с уведомлением об успехе или отказе. PAN, CVV и срок
действия карты в среде мерчанта не появляются, поэтому именно этот
путь чаще всего ведёт к SAQ A \sidecite{pci-saq}.

Недостаток здесь тоже очевиден: покупатель замечает смену домена
в адресной строке, а вместе с ней и разрыв пользовательского
сценария. Это может вызывать недоверие и повышать долю
незавершённых заказов. Современные PSP смягчают эффект, предлагая
стилизованные страницы оплаты, визуально близкие к сайту мерчанта.
Но для малого бизнеса и для всех случаев, где упрощение комплаенса
важнее бесшовности интерфейса, redirect по-прежнему остаётся
предпочтительным выбором \sidecite{pci-saq}.

Именно поэтому выбор между полным перенаправлением, вынесенными полями
и собственной
платёжной страницей — это не только вопрос UX, но и решение
о масштабе будущего комплаенса. Между SAQ D и SAQ A лежит разный
объём архитектурной, операционной и документарной работы.

\begin{figure}
  \centering
  \includegraphics[width=\linewidth]{assets/figures/ch12-pci-dss/pci-scope-reduction.pdf}
  \caption{Сужение PCI-периметра от SAQ D к SAQ A.}
  \label{fig:pci-scope-reduction}
\end{figure}

\section{SAQ A, SAQ A-EP и SAQ D}
\label{sec:pci-saq}

SAQ (Self-Assessment Questionnaire) — форма самооценки, которую
PCI SSC предоставляет мерчантам для подтверждения соответствия
без полного аудиторского цикла с участием QSA. Тип SAQ определяется
не размером компании и не отраслью, а конкретным способом
взаимодействия с карточными данными: каким образом карта попадает
в платёжный процесс и соприкасается ли мерчант с CHD
\sidecite{pci-saq}. Именно поэтому тип SAQ\sidenote{SAQ
(Self-Assessment Questionnaire) — форма самооценки PCI SSC.
Тип вопросника зависит от того, как конкретная архитектура
соприкасается с данными карты.} нужно читать как архитектурный
вывод, а не как категорию бизнеса.

Интернет-магазин, который при оплате полностью перенаправляет
покупателя на страницу PSP по сценарию redirect, как правило,
использует \textbf{SAQ A} — наиболее узкий вопросник
для типичного электронного мерчанта с вынесенной страницей оплаты.
Но SAQ A не означает нулевой периметр: PCI SSC отдельно уточняет,
что веб-сервер мерчанта, на котором расположен механизм redirect,
остаётся в периметре, чтобы проверяющий мог проверить его конфигурацию
и сам механизм перенаправления \sidecite{pci-faq-1332}.

Сценарий со встроенным iFrame тоньше. Если все элементы платёжной
страницы, которые получает браузер, исходят только и напрямую
от PCI DSS-совместимого сервис-провайдера, реализация может
оставаться в SAQ A. Если же хотя бы часть элементов страницы
формируется сайтом мерчанта, получается \textbf{SAQ A-EP}. Именно
этим PCI SSC и объясняет различие между Direct Post и iframe/URL
redirect: в Direct Post сайт мерчанта сам участвует в приёме
карточных данных, тогда как в iframe и redirect страницу для ввода
реквизитов формирует процессор
\sidecite{pci-faq-1291}\sidecite{pci-faq-1293}. Для встроенной
формы в SAQ A r1 появилось ещё одно условие: мерчант должен
подтвердить, что его страница не подвержена атакам через скрипты;
для чистого redirect и полностью вынесенной оплаты это условие
не применяется \sidecite{pci-faq-1588}.

Для сценариев с физическими терминалами действуют отдельные типы
вопросников. \textbf{SAQ B-IP} применяется к торговым точкам
с PTS-сертифицированными терминалами, подключёнными к платёжному
процессору по IP. Для PCI DSS v4 PCI SSC отдельно уточнил,
что этот тип рассчитан на автономные point-of-interaction (POI)
устройства, одобренные PCI SSC и не смешанные с другими типами
устройств в той же сетевой зоне
\sidecite{pci-faq-1443}.

\textbf{SAQ C-VT} покрывает другой класс сценариев: колл-центр
или бэк-офис, где сотрудник вручную вводит карточные данные
через виртуальный терминал PSP, то есть через веб-интерфейс,
который сам не хранит CHD. Официальное FAQ PCI SSC дополнительно
оговаривает, что речь идёт об изолированном компьютере с браузером,
через который вводится по одной транзакции за раз
\sidecite{pci-faq-1229}.

Если архитектуру нельзя свести ни к одному из упрощённых сценариев,
остаётся \textbf{SAQ D}. Он применяется ко всем мерчантам, которые
не подходят ни под один из специальных типов, а также ко всем
сервис-провайдерам без исключения. SAQ D соответствует полному
объёму требований PCI DSS. Разница между SAQ A и SAQ D — не в том,
что одна анкета толще другой, а в масштабе операционной нагрузки:
больше тестов, больше документации, ежеквартальные ASV-сканы,
ежегодный тест на проникновение. Для многих мерчантов вложение
в архитектуру, позволяющую перейти с SAQ D на SAQ A, окупается
уже в первый комплаенс-цикл \sidecite{pci-saq}.

\begin{figure}
  \centering
  \includegraphics[width=\linewidth]{assets/figures/ch12-pci-dss/pci-saq-decision.pdf}
  \caption{Дерево выбора типа SAQ.}
  \label{fig:pci-saq-decision}
\end{figure}

\section{PCI DSS 4.0.1: что особенно ощущается на практике}
\label{sec:pci-401}

В версии 4.0.1 особенно заметны требования, которые бьют
не по формальной документации, а по повседневной эксплуатации:
по странице оплаты, по инвентарю скриптов и по доказуемости
внутренних процедур \sidecite{pci-dss-4}.

\begin{kaobox}[title=Важно, colback=red!5, colbacktitle=red!20]
  Два требования, которые индустрия почувствовала особенно остро:

  \textbf{Требование 6.4.3: инвентаризация и авторизация скриптов
    на платёжной странице.} Каждый скрипт JavaScript, загружаемый
  на страницу, где вводятся карточные данные, должен быть
  инвентаризирован, обоснован и авторизован. Для интернет-торговли
  это болезненно: типичная платёжная страница подгружает десятки
  скриптов — аналитику, системы A/B-тестирования, менеджеры тегов,
  чат-виджеты, — и каждый из них теперь требует документирования
  и контроля целостности (Content Security Policy, Subresource
  Integrity). Требование направлено против атак типа Magecart,
  при которых злоумышленник внедряет вредоносный скрипт
  на платёжную страницу для перехвата карточных данных.

  \textbf{Требование 11.6.1: выявление несанкционированных
    изменений на платёжной странице.} Организация должна
  обнаруживать модификации HTTP-заголовков и содержимого
  платёжной страницы, которые могут привести к краже карточных
  данных. На практике это означает контроль целостности,
  мониторинг клиентской стороны и процедуры, рассчитанные именно
  на атаки электронного скимминга.
\end{kaobox}

Помимо этого, версия 4.0.1 усилила ещё несколько направлений:

\begin{itemize}
  \item Многофакторная аутентификация (MFA) обязательна для всех
        обращений к CDE, а не только для удалённого доступа.
  \item Внутренние сканы уязвимостей должны выполняться
        в аутентифицированном режиме, чтобы охватывать
        конфигурационные и локальные проблемы, невидимые извне.
  \item Для защиты PAN при передаче стандарт теперь жёстче
        фиксирует управление доверенными ключами и сертификатами:
        их нужно инвентаризировать, принимать только доверенные
        экземпляры и контролировать их валидность.
  \item Целевой анализ риска (Targeted Risk Analysis): организация
        должна документировать обоснование периодичности для тех
        контролей, где стандарт допускает гибкость, например для
        частоты пересмотра правил межсетевого экрана.
\end{itemize}

Общий вектор здесь прозрачен: PCI DSS всё меньше довольствуется
декларациями и всё больше требует доказуемой управляемости среды.
Большинство новых требований версии 4.0 стали обязательными
после 31 марта 2025 года, когда завершился переходный период,
в течение которого их ещё можно было трактовать как будущие
обязательные практики
\sidecite{pci-dss-4}.

\section{Альтернативный путь: Customized Approach}
\label{sec:pci-customized}

PCI DSS 4.0 ввёл альтернативный путь выполнения требований —
Customized Approach. Его идея проста: не обязательно реализовывать
контроль буквально так, как он описан в стандарте, если организация
может другим способом достичь той же цели безопасности и доказать
это на проверке. Иными словами, стандарт допускает инженерную
свободу, но только вместе с доказательной дисциплиной.

Для использования Customized Approach необходимо:

\begin{itemize}
  \item Определить цель безопасности, указанную в стандарте
        для соответствующего требования.
  \item Провести целевой анализ риска (Targeted Risk Analysis),
        показывающий, что
        альтернативный контроль достигает той же цели.
  \item Предоставить аудитору QSA (Qualified Security Assessor)
        документацию и доказательства, достаточные для валидации.
\end{itemize}

Это дорогой инженерный инструмент. Он требует
зрелой программы ИБ, опытного QSA и готовности вкладываться
в доказательную базу. Для типовой архитектуры предписанный путь
(Defined Approach) обычно проще и дешевле. Альтернативный путь
имеет смысл там, где архитектура не укладывается в стандартный
шаблон, но безопасность всё же можно убедительно показать.

\section{PCI DSS и российские требования: \law{821-П}, \law{851-П}}
\label{sec:pci-russian}

Для российской организации, работающей с карточными платежами,
PCI DSS никогда не существует в одиночку. Поверх требований
карточных сетей действуют положения Банка России \law{821-П}
и \law{851-П}, которые мы разбирали в главе \ref{ch:regulation}.
\law{821-П} задаёт базовый контур защиты информации при переводе
денежных средств, а \law{851-П} — более широкий контур ИБ кредитной
организации и мер против операций без согласия клиента
\sidecite{cbr-821p}\sidecite{cbr-851p}. Исторически тот же разговор
часто вели через \law{382-П} и \law{683-П}, но в этой книге они
нужны только как предшествующая нормативная рамка.

Пересечение между режимами велико:

\begin{itemize}
  \item \textbf{Шифрование данных} — и PCI DSS,
        и \law{821-П}/\law{851-П} требуют криптографической защиты.
        Но российская рамка дополнительно опирается на
        сертифицированные СКЗИ и ГОСТ-алгоритмы там, где это
        предписано регулятором
        (глава \ref{ch:crypto}), тогда как PCI DSS
        не предписывает конкретный алгоритм.
  \item \textbf{Контроль доступа} — оба режима требуют разграничения
        доступа, идентификации пользователей, MFA.
  \item \textbf{Мониторинг и журналирование} — требования 10--12
        PCI DSS
        и требования \law{821-П}/\law{851-П} к регистрации событий,
        реагированию на инциденты и контролю защищённости.
  \item \textbf{Управление уязвимостями} — требования 5--6 PCI DSS
        и требования \law{851-П} к анализу уязвимостей,
        безопасной разработке и оценке применяемого ПО.
\end{itemize}

Но полного совпадения нет, и именно на расхождениях обычно
ломаются бюджет, архитектура и план валидации:

\begin{itemize}
  \item \textbf{Криптография.} PCI DSS допускает AES, RSA
        и другую \enquote{strong cryptography}. \law{821-П}
        и \law{851-П} в ряде контуров требуют российские
        криптографические механизмы и сертифицированные средства
        защиты \sidecite{cbr-821p}\sidecite{cbr-851p}.
  \item \textbf{Отчётность и доказательства.} PCI DSS предполагает
        отчёт перед карточной сетью или эквайером через ROC, AOC
        или SAQ. Российская рамка добавляет собственную оценку
        соответствия, регуляторную отчётность и надзор со стороны
        Банка России.
  \item \textbf{Оценка соответствия ПО.} \law{851-П} требует
        для ряда категорий прикладного ПО, используемого
        при банковских операциях, либо сертификации, либо оценки
        соответствия не ниже ОУД4; для части иного прикладного ПО
        необходимость такой оценки банк определяет сам
        \sidecite{cbr-851p}.
\end{itemize}

Отдельного пояснения заслуживает требование \law{851-П}
об оценке соответствия программного обеспечения по ОУД4.
ОУД4 (Оценочный уровень доверия 4) определён ГОСТ Р ИСО/МЭК
15408-3, то есть российской адаптацией Common Criteria,
и фигурирует в \law{851-П} не как тотальное требование ко всему ПО
банка, а как часть более точного режима для прикладного
программного обеспечения автоматизированных систем и приложений,
а также отдельного ПО, которое используется для банковских
операций и обрабатывает защищаемую информацию. Для этих категорий
Положение требует либо сертификации, либо оценки соответствия
не ниже ОУД4; для иного прикладного ПО банк вправе сам определить,
нужна ли такая процедура. Пункт 4.2 отдельно допускает, что оценка
может проводиться либо самой организацией, либо с привлечением
лицензированной проверяющей организации \sidecite{cbr-851p}.

Прямого аналога этому в PCI DSS нет. Требование 6 PCI DSS
описывает внутренние процессы безопасной разработки, управление
уязвимостями и жизненный цикл изменений, но не вводит
сопоставимого внешнего режима сертификации или формальной оценки
соответствия для прикладного банковского ПО. Практическое
следствие поэтому уже, чем часто пересказывают: речь идёт
не обо всём технологическом стеке подряд, а о тех классах
банковских приложений, которые попадают в формальный контур
\law{851-П} \sidecite{cbr-851p}.

\begin{kaobox}[title=На практике]
  Единственный разумный подход к параллельному комплаенсу —
  построить общую матрицу контролей, в которой каждому требованию
  PCI DSS поставлены в соответствие пункты \law{821-П}
  и \law{851-П}. Работа ведётся в три слоя: сначала выделяются
  контроли, удовлетворяющие оба режима одновременно (контроль
  доступа, журналирование, сетевая сегментация, управление
  инцидентами) — как правило, это большинство. Затем
  фиксируются расхождения в технических требованиях:
  криптографические операции внутри периметра реализуются
  на ГОСТ-алгоритмах с сертифицированными СКЗИ, тогда как тот же
  трафик может дополнительно защищаться TLS для соответствия
  требованию 4. Наконец, документируются требования, не имеющие
  пересечения: сертификация или оценка соответствия прикладного ПО
  (только для \law{851-П}), регуляторная отчётность и контроль Банка России
  (только для \law{821-П}/\law{851-П}), SAQ или ROC отчётность
  перед карточной сетью (только PCI DSS).
  Единый годовой аудит по объединённой программе — с единой
  доказательной базой и одним QSA, понимающим российское
  регулирование, — обычно заметно дешевле двух параллельных
  процессов.
\end{kaobox}

\begin{chaptersummary}
\begin{itemize}
  \item PCI DSS — это двенадцать требований, собранных в шесть
        групп; их логика состоит в концентрической обороне вокруг
        карточных данных, а не в механическом чеклисте.
  \item CDE — всё, что хранит, обрабатывает или передаёт CHD/SAD,
        плюс непосредственно связанные и влияющие на безопасность
        системы. Сегментация сети — ключевой инструмент ограничения
        периметра оценки.
  \item Сужение периметра через токенизацию, P2PE, вынесенные поля
        оплаты и полное перенаправление радикально меняет объём
        требований: между SAQ D и SAQ A лежит архитектурная разница.
  \item PCI DSS 4.0.1 ужесточает практику эксплуатации: требует
        инвентаризации скриптов на платёжной странице, аутентифицированных
        внутренних сканов, MFA для всех обращений к CDE
        и более формализованного управления ключами и сертификатами
        для защиты PAN при передаче.
  \item Customized Approach позволяет достичь цели безопасности
        альтернативным контролем, но требует зрелой программы ИБ,
        анализа риска и убедительной валидации со стороны QSA.
  \item В России PCI DSS и положения \law{821-П} и \law{851-П}
        действуют параллельно: они пересекаются в шифровании,
        контроле доступа и мониторинге, но расходятся в криптографии,
        регуляторной отчётности и формальном контуре оценки
        соответствия прикладного ПО. Практически это означает
        необходимость единой матрицы контролей.
\end{itemize}
\end{chaptersummary}
