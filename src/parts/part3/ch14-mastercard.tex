
\chapter{Mastercard}~\label{ch:mastercard}
Mastercard часто воспринимается как зеркало Visa: та же
четырёхсторонняя модель, тот же EMV, те же таблицы interchange.
Но аналогия быстро перестаёт помогать в местах, где сеть
расходится с Visa -- в единой сетевой архитектуре, форматах GCMS/IPM,
токенизации через MDES, механизме DSRP и сервисе ABU.

\section{Сетевой периметр Mastercard}~\label{sec:mc-banknet}

Фразу \enquote{у Mastercard одна сеть вместо двух} легко понять
слишком буквально. Граница для участника проходит между сетью,
прикладной подсистемой и набором правил. При обычной покупке
эквайер отправляет авторизационный запрос и ждёт ответ за секунды.
Затем сеть принимает клиринговые данные и сводит взаимные
обязательства банков. В актуальных
публичных материалах Mastercard этот стек описывается через
Switching Services: authorization, clearing и settlement обслуживаются
Authorization Platform, системой Global Clearing Management System
(GCMS) и системой Settlement Account Management (SAM)~\cite{mc-switching-services}.

Именно это и означает \enquote{единая сеть}: участник работает с единым
сетевым доменом Mastercard, внутри которого авторизация, клиринг
и разные задачи разведены по специализированным подсистемам
с собственными форматами и жизненными циклами.

% TODO_ILLUSTRATE: Топология логических подсистем сетевого периметра Mastercard.
%\begin{figure}[tbp]
%  \centering
%  \includefiguretex[width=\linewidth]{assets/figures/ch14-mastercard/banknet-topology}
%  \caption{Топология логических подсистем сетевого периметра Mastercard.}~\label{fig:banknet-topology}
%\end{figure}

Такое описание упрощает разговор об архитектуре, но не отменяет
различий между потоками. Авторизация,
clearing и settlement по-прежнему живут в разном временном режиме,
подчиняются разным окнам обработки и опираются на разные форматы
данных. Следовательно, сравнивать Mastercard с Visa
нужно по тому, как именно сеть разводит эти функции внутри себя,
а не по числу сетей на схеме.

Если у обеих схем авторизация и клиринг разведены во времени,
остаётся выяснить, чем именно модель dual message у Mastercard
отличается от привычной пары BASE I / BASE II.

\section{Модель dual message в Mastercard: почему IPM не равен TC33}~\label{sec:mc-dmsa}

Граница между авторизацией и клирингом в Mastercard заслуживает
отдельного разбора.
Публичные документы самой сети помогают опереться на конкретику: в модели
двух сообщений (dual message) авторизационные сообщения обрабатываются
через Authorization Platform, а клиринговые записи направляются в GCMS,
где данные конвертируются в формат IPM для clearing и settlement~\cite{mc-chargeback-guide}.

Это снимает распространённую путаницу. У Mastercard второе
сообщение не исчезает и не \enquote{растворяется} в общей сети; оно
просто живёт в другой подсистеме и в другом формате. Если
авторизационный диалог по-прежнему выглядит знакомо и строится вокруг
MTI~0100 и MTI~0110, то на клиринговом этапе эквайер формирует для
GCMS интегрированные продуктовые сообщения (Integrated Product Messages,
IPM), а не отправляет TC33~\cite{mc-chargeback-guide}.

На практике это особенно видно на первом предъявлении. В модели dual
message эквайер после авторизации готовит первое предъявление (first
presentment) в формате IPM; именно эту сущность и нужно сравнивать
с клиринговым конвейером Visa.

\importantnote{%
Dual message у Visa и Mastercard похож по идее, но инженерная единица
клиринга разная: у одной сети BASE II/TC33, у другой GCMS/IPM. Ошибка
начинается, когда IPM трактуют как переименованный TC33.

Для разработчика из этого следует вполне конкретное ограничение:
клиринговый конвейер Mastercard требует отдельной логики формирования
IPM, отдельной валидации и отдельного разбора ответов клиринговой
системы. Иначе проблемы проявятся на сверке, корректировках
и settlement.
}

Общая логика dual message -- разделение авторизации и клиринга
во времени -- разобрана в главе~\ref{ch:tx-lifecycle};
здесь важны именно технические детали реализации на стороне
Mastercard.
\section{IPM и GCMS}~\label{sec:mc-ipm}

Если предыдущее различие было архитектурным, то здесь вопрос
становится прикладным: что именно видит инженер в клиринговом
контуре Mastercard? GCMS принимает и маршрутизирует данные клиринга в формате
IPM -- Integrated Product Messages,
то есть интегрированные продуктовые сообщения для клиринга и связанных
с ним операционных процессов~\cite{mc-switching-services}.

Здесь важно удержать два инварианта. Во-первых, IPM -- структурированный
сетевой формат с типами сообщений и нотацией элементов данных~\cite{mc-chargeback-guide}.
Во-вторых, подробная карта записей, подэлементов (subelements)
и служебных заголовков вынесена в специализированную документацию
Mastercard; публичные материалы сети надёжно
подтверждают сам формат IPM и роль GCMS, но не дают полного атласа
полей для построчного разбора.

\begin{table}[tbp]
  \centering
  \caption{Структура IPM-обмена Mastercard.}~\label{fig:mc-ipm-structure}
  \begin{tabular}{@{}ll@{}}
    \toprule
    \textbf{Уровень}              & \textbf{Описание}                                          \\
    \midrule
    IPM-файл                      & Контейнер клирингового обмена                              \\
    Клиринговая запись            & Единица обмена внутри файла                                \\
    Message header                & Служебный заголовок записи                                 \\
    ISO 8583 data elements        & Элементы данных транзакции                                 \\
    Fees / adjustments            & Комиссии и корректировки                                   \\
    \bottomrule
  \end{tabular}
\end{table}

Одна деталь из публичных правил особенно важна для реальной
эксплуатации. Код рекомендации мерчанту (Merchant Advice Code, MAC)
-- это совет, который сеть передаёт в авторизационном
ответе в DE~48, subelement 84 для поддерживаемых сценариев~\cite{mc-tpr}. Смысл этого поля не косметический:
по нему мерчант или шлюз решает, стоит ли повторять попытку,
обновлять реквизиты или прекращать автоматические списания.
Игнорировать MAC -- значит строить логику повторов вслепую.

Для разработчика вывод здесь такой. На этапе диагностики
нужно различать две плоскости: IPM и GCMS отвечают за клиринговый
обмен и последующую обработку, а рекомендации по повторам часто
приходят раньше -- ещё в авторизационном ответе. Дальше сеть
использует результаты clearing для расчёта нетто-позиций и передачи
их в контур settlement, но конкретные окна и порядок урегулирования
зависят от правил региона и роли участника~\cite{mc-rules}.

\section{MDES и токенизация}~\label{sec:mc-mdes}

Поверх общей схемы EMVCo Mastercard добавляет сервис
MDES (Mastercard Digital Enablement Service): вместо исходного PAN
в транзакции используется платёжный токен, а сеть управляет
его жизненным циклом и правилами применения~\cite{mc-mdes-token-connect}.

MDES включает механизм защищённой удалённой оплаты Digitized Secure
Remote Payment (DSRP) -- способ принимать интернет-платежи и платежи в
приложениях
с опорой на EMV-криптографию~\cite{mc-tpr}. Для инженера важен именно
этот акцент:
DSRP даёт эмитенту более сильный криптографический сигнал,
чем обычный сценарий сохранённых реквизитов.

Граница здесь тоже проходит чётко. Публичные материалы сети
уверенно подтверждают роль MDES и DSRP, но не позволяют утверждать,
будто любой клиентский кошелёк всегда формирует
один и тот же набор криптограмм и полей. Поэтому в книге достаточно
зафиксировать инвариант: у Mastercard токенизация и удалённая
оплата теснее связаны с EMV-криптографией, чем в упрощённой модели
\enquote{токен вместо PAN}.

\section{M/Chip: семейство EMV-реализаций}~\label{sec:mc-mchip}

Общий стандарт EMV подробно разобран в главе~\ref{ch:emv},
а здесь важнее другой вопрос: почему общего знания EMV недостаточно,
когда устройство, карта или процессинг должны поддерживать именно
Mastercard? Ответом и служит семейство требований M/Chip~\cite{mc-mchip}.

Практический смысл M/Chip не в перечне профилей как таковом,
а в том, что Mastercard поверх общего стандарта EMV задаёт
собственные требования к поведению приложения, данным, тестированию
и сертификации. Поэтому утверждение \enquote{терминал уже поддерживает
EMV, значит Mastercard заработает автоматически} слишком оптимистично.
Нужны именно схема-специфичные требования и их проверка.

\importantnote{%
M/Chip -- семейство требований Mastercard. Для эквайера и вендора
терминалов важен
конкретный набор тестов, сертификатов и поведенческих требований.

}
\paragraph{Проверка по первоисточнику}
если понадобится точная и актуальная матрица
профилей M/Chip и областей их применения, сверять текущие
M/Chip Requirements for Contact and Contactless через
Mastercard Connect; в открытом доступе полной матрицы нет.
\section{Mastercard Send}~\label{sec:mc-send}

Когда деньги нужно быстро доставить получателю, а не списать с карты,
на первый план выходит выплата, а не покупка. Mastercard Send
закрывает именно такие задачи: выплаты и переводы, где средства
должны уйти получателю на карту, банковский счёт, цифровой кошелёк
или другой канал, который поддерживает конкретная программа~\cite{mc-send-aptpay}.

Для карточного сценария удобен канонический пример:
сначала отправитель инициирует финансирующую транзакцию (Funding
Transaction), а затем платформа запускает платёжную транзакцию
(Payment Transaction) в пользу получателя~\cite{mc-tpr}.
Публичные материалы Mastercard надёжно подтверждают прежде всего near
real-time характер сервиса и вариативность каналов доставки;
конкретная схема зависит от страны и программы~\cite{mc-send-aptpay}.

Именно здесь проходит эксплуатационная граница. Обещание
\enquote{почти мгновенной выплаты} описывает возможность сети,
но не заменяет проверку конкретного платёжного коридора, банка
или провайдера-получателя и режима их работы. Продуктовой команде
важно сверять SLA под каждый рынок отдельно, даже если бренд
сервиса один и тот же.

\practicenote{%
При построении платформы выплат через Mastercard Send важно заранее
проверить наличие у сети нужного платёжного коридора и готовность
банка или провайдера-получателя принимать такие переводы в нужном режиме.
Режим near real-time в маркетинговом описании сервиса и фактическая
доступность средств получателю -- не одно и то же. Для платформ
выплат это различие критично: именно на нём ломаются обещания
\enquote{деньги за минуту}.

}
\section{ABU: автоматическое обновление данных карты}~\label{sec:mc-abu}

У мерчанта с подписками одна из самых неприятных проблем выглядит
буднично: карта клиента перевыпущена, срок действия изменился,
а запланированные списания начинают падать. Автоматическое обновление
реквизитов (Automatic Billing Updater, ABU) -- сервис
Mastercard, который нужен именно для этой ситуации~\cite{mc-abu}.

Суть механизма проста. Эмитент сообщает сети об изменении реквизитов,
а участник на стороне мерчанта получает обновлённые платёжные данные
для сценариев хранения реквизитов (account-on-file) и рекуррентных
списаний~\cite{mc-tpr}.
Важно, что речь идёт не о волшебной синхронизации всех клиентских
данных, а о контролируемом обновлении платёжных реквизитов,
необходимых для продолжения уже разрешённых списаний.

Эта оговорка важна технически. Публичные правила подтверждают роль ABU
и его связь с сохранёнными реквизитами, но не раскрывают единый
универсальный API для всех ролей участника. Поэтому на уровне книги
достаточно понимать инвариант: после перевыпуска карты ABU помогает
сократить число ложных отказов там, где право на дальнейшие списания
у мерчанта уже есть.

Аналог у Visa -- Visa Account Updater (VAU): архитектурно
схожий сервис с теми же целями. Принципиальное различие -- техническое:
ABU и VAU остаются разными сервисами с разными операционными
процедурами. Платформа, работающая в обеих сетях, не может
поддержать \enquote{обновление реквизитов вообще}; ей нужно поддержать
оба механизма отдельно. Тема связи ABU с управлением подписками --
в главе~\ref{ch:subscriptions} (Часть IV).

С продуктовой точки зрения ABU -- инструмент сохранения непрерывности
выручки. Для мерчанта
с подписками он влияет на долю успешных повторных списаний,
на потери после перевыпуска карты и на необходимость вручную
возвращать клиента в платёжный поток.

\section{Interchange Mastercard}~\label{sec:mc-interchange}

Одна и та же покупка для мерчанта оказывается \enquote{дороже}
или \enquote{дешевле} в зависимости от карты и канала. Как и у
Visa, interchange у Mastercard определяется сочетанием региона,
продукта, MCC, канала приёма и квалифицирующих условий транзакции~\cite{mc-interchange-us}.

С инженерной точки зрения здесь важны
логика квалификации и чтения таблицы. Сначала выбирается региональная таблица,
потом тип продукта и категория мерчанта, затем проверяются признаки,
от которых зависит квалификация транзакции: какие данные переданы,
была ли операция электронной, использовались ли корректные карточные
или токенные реквизиты. Именно здесь возникают понижения категории
(downgrade)
и связанные с ними споры между мерчантом, эквайером и процессором.

Предел точности здесь нужно оговорить честно. Без привязки
к конкретному рынку, дате публикации и публичной региональной таблице
нельзя надёжно утверждать, что тот или иной премиальный продукт
Mastercard \emph{всегда} дороже сопоставимого продукта
Visa. Такие сравнения быстро превращаются в смесь локальных
правил, частных договорённостей и устаревших тарифных редакций.
Для рынка США Mastercard публикует отдельные таблицы ставок
и критериев interchange; любой численный пример в книге нужно
привязывать именно к такой конкретной региональной таблице~\cite{mc-interchange-us}.

\practicenote{%
При сравнении interchange Mastercard и Visa для конкретного
мерчанта ключевой параметр -- фактический микс продуктов и
качество данных в транзакции.
Диагностику лучше вести в таком порядке: выбрать региональную
таблицу, определить продукт карты, проверить MCC и затем сверить,
выполнила ли транзакция квалифицирующие условия. Если спорить
со ставкой раньше этих четырёх шагов, разговор почти всегда
уходит в догадки.

}
\section{Архитектурные отличия от Visa}~\label{sec:mc-vs-visa}

Теперь сравнение можно собрать в одну картину: многие различия между
Mastercard и Visa -- действительно терминологические
или исторические, но несколько из них существенны именно с инженерной
точки зрения.

\paragraph{Архитектурные отличия Mastercard и Visa}
\begin{center}
  \begin{tabularx}{\textwidth}{@{}>{\RaggedRight\arraybackslash}p{0.26\textwidth}>{\RaggedRight\arraybackslash}X>{\RaggedRight\arraybackslash}X@{}}
    \toprule
    \textbf{Параметр}     & \textbf{Mastercard}
    & \textbf{Visa}                                             \\
    \midrule
    Сетевая архитектура   & Единый сетевой периметр Mastercard
    & BASE I для авторизации и BASE II для клиринга             \\
    Клиринг               & IPM через GCMS
    & TC33 через BASE II                                        \\
    Модель dual message   & Авторизация и клиринг разведены внутри
    одной сети & Авторизация и клиринг разведены по двум именованным сетям \\
    Токенизация           & MDES + DSRP
    & VTS + Token Assurance Level                               \\
    Чиповые требования    & M/Chip
    & Visa ICC / VCPS                                           \\
    Выплаты               & Mastercard Send
    & Visa Direct                                               \\
    Обновление реквизитов & Automatic Billing Updater (ABU)
    & Visa Account Updater (VAU)                                \\
    \bottomrule
  \end{tabularx}
\end{center}

Наиболее существенны различия в клиринговой модели и формате обмена.
Разработчик, который переносит систему обработки клиринга с одной
схемы на другую, не может просто переименовать поля: структуры IPM
и TC33 различаются настолько, что требуют отдельного парсинга,
маппинга и отдельной операционной диагностики. Различия
в токенизации важны для разработчиков кошельков и интернет-платёжных
интеграций. Различия в ABU и VAU -- для платёжных платформ
с рекуррентными платежами.

Есть и различия другого рода: одни и те же отраслевые слова у схем
иногда обозначают не совсем одно и то же. Термин \enquote{dual message}
применяют и к Visa, и к Mastercard, но за ним стоят
разные технические реализации. Нейтральная формула \enquote{авторизация
и клиринг разделены во времени} верна для обеих схем; детали
этого разделения -- разные.

\practicenote{%
При работе с IPM-сообщениями Mastercard для диагностики отказов
нужно смотреть на сам факт отказа и на код рекомендации мерчанту
(Merchant Advice Code) в авторизационном
ответе: именно он подсказывает, допустимы
ли повторы и нужно ли обновлять сохранённые реквизиты. Иными словами,
IPM нужен для клиринга и последующей обработки, а оперативная
логика повторов часто определяется ещё на авторизационном шаге~\cite{mc-tpr}.

}
\chapterSummary\par
\begin{itemize}
  \item Публичные материалы Mastercard описывают единый сетевой
    периметр через Authorization Platform, GCMS и расчётную систему
    SAM; авторизация, clearing и settlement внутри него разведены по
    разным подсистемам.
  \item Модель dual message в Mastercard требует отдельного описания:
    клиринговый обмен идёт в формате IPM через GCMS, а не через TC33.
  \item Merchant Advice Code приходит в авторизационном ответе и
    управляет логикой повторов; важно держать этот слой отдельно от
    клирингового разбора.
  \item MDES и DSRP показывают, что у Mastercard токенизация тесно
    связана с EMV-криптографией и удалённой оплатой.
  \item M/Chip задаёт схема-специфичные требования к чиповым
    продуктам; точную матрицу профилей нужно сверять по актуальной
    документации Mastercard.
  \item Mastercard Send -- сервис выплат с возможностью почти
    мгновенного зачисления; фактический SLA зависит от конкретного
    рынка, банка и платёжного коридора.
  \item ABU автоматически обновляет платёжные реквизиты для сценариев
    хранения реквизитов и рекуррентных списаний; аналог Visa -- VAU.
  \item Interchange у Mastercard определяется регионом, продуктом,
    MCC и качеством данных; без привязки к конкретной таблице
    численные сравнения с Visa ненадёжны.
\end{itemize}
