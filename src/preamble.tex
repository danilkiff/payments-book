% Пакеты LaTeX и глобальные настройки книги.
% Редактору: этот файл не нужно трогать при добавлении глав или фигур.
% Стили TikZ живут в src/figure-styles.tex (подключаются отсюда же).
%
% Keep the preamble focused on functionality; visual defaults come from KOMA.

\usepackage[T2A,T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[main=russian,english]{babel}
\usepackage{libertinus}
\renewcommand{\ttdefault}{cmtt}

\usepackage{graphicx}
\usepackage{adjustbox}
\usepackage{tabularx}
\usepackage{ragged2e}
\usepackage{booktabs}
\usepackage{csquotes}
\usepackage[backend=biber,style=numeric-comp,sorting=nyt]{biblatex}
\usepackage[unicode]{hyperref}
\input{src/figure-styles}

\newcommand{\includefiguretex}[2][width=\linewidth]{%
  \adjustbox{#1}{\sffamily\input{#2}}%
}

\hypersetup{
  pdftitle={Платёжный стек},
  pdfauthor={},
  hidelinks,
}

\setcounter{tocdepth}{1}
